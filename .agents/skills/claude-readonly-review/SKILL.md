---
name: claude-readonly-review
description: Dispatch one exact-ref packet to the commissioned isolated Claude advisory plane and prove zero repository mutation. Use only for Director-approved C9A on-demand independent review.
---

1. Recover current authority first. C9A creates no Engineering, Design, provider, validation, product, ODR, merge, deployment, scheduling, background-execution, or mutation authority.
2. Select one bounded review question and explicit text paths at one exact commit. Do not expose a live checkout or ask Claude to discover context.
3. Run `& $env:ENSEMBLE_PYTHON -I -B tools/codex-admin-claude-review.py --repo <exact-git-root> --ref <exact-ref> --path <path> --question <bounded-question> --scratch-root C:\Users\Wiryl\.codex-ensemble\scratch\c9a --claude-exe C:\Users\Wiryl\AppData\Local\Microsoft\WinGet\Links\claude.exe --claude-config-dir C:\Users\Wiryl\.claude-ensemble`, repeating `--path` only for the frozen packet.
4. Require first-party `claude.ai` Pro authentication after alternate API/cloud routes are removed from the child environment. Tools, inherited MCP, permission prompts, session persistence, Chrome, slash commands, fallback routing, and automatic retry remain unavailable.
5. Require packet/result hashes, `--max-turns 1`, ordinary JSON output parsed and schema-validated locally (do not use Claude native `--json-schema`), zero permission denials, and identical Git status/ref/worktree snapshots before and after. Timeout or any mismatch fails closed with sealed telemetry; do not retry automatically.
6. Claude findings are advisory. Return them to the owning Sol/Administrator for reconciliation; never implement, rewrite authority, or perform a side effect because the reviewer recommended it.
