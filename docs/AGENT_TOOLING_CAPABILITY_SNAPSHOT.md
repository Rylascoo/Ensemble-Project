# Ensemble Agent Tooling Capability Snapshot

Status: ACTIVE VOLATILE OPERATING SNAPSHOT — NOT PRODUCT OR ARCHITECTURE LAW

Verified: 2026-09-11

## Purpose

This file records the current recommended model/tool allocation for the project. It is intentionally volatile and may be updated without changing product, engineering, design, provider, or validation authority.

Before a scarce/external dispatch, verify current model availability, effort controls, plan limits, billing behavior, and tool capability. A historical menu label must never become constitutional law.

## Current manager surfaces

| Role | Surface | Current recommendation | Use |
|---|---|---|---|
| Engineering Sol | ChatGPT project chat | GPT-5.6 Sol — High | engineering management, architecture, evidence interpretation, recursive audit, authority reconciliation |
| Design Sol | ChatGPT project chat | GPT-5.6 Sol — High | design management, method/visual interpretation, recursive audit, authority reconciliation |

For small factual/status work, a lower reasoning setting may be adequate. For material architecture, broad failure analysis, security/provider/Store work, or cross-project reconciliation, prefer High.

## Codex allocation - C0/C1/C2/C3/C4/C5/C6/C7/C8/C9 commissioned; C9A R2 consumed/falsified; C10 blocked/falsified

C0 commissioned the dedicated `C:\Users\Wiryl\.codex-ensemble` runtime; C1 proved the versioned read-only repository reconciler; C2 proved fresh authority recovery after supported elevated Windows sandbox provisioning; C3 proved the eight repository-native deterministic Skills, fixtures, negative controls, manual cross-checks, and full-result exact-SHA CI normalization without authority invention. The runtime denies login shells, keeps filesystem/network restricted with approval `OnRequest`, disables the remote plugin catalog, and admits only GitHub `fetch` from an otherwise app-denied posture. C4 proved native child permission inheritance stays read-only and commissioned the prescribed separate top-level Worker fallback. The Administrator remains read-only/on-request; Worker launch is exact-worktree/HEAD bound, workspace-write, approval `never`, command-network off, app/subagent/browser/provider surfaces disabled, with shared Git lifecycle retained by the Administrator. C5 commissioned the per-common-Git-directory atomic shared-Git lock and proved the disposable branch/tag/push/PR lifecycle with race checks, PR-only `main` integration, archive-tag verification, and safe worktree/branch disposal. C6 proved GitHub Issues can carry an exact-ref cross-lane dispatch packet and bounded result as ephemeral transport without labels, milestones, assignees, repository mutation, or independent backlog/status truth; Issues are admitted only in that transport role. C7 proved read-only analysis of sealed Run-03 evidence: all 62 recorded artifact hashes, runtime root, seal identity, request accounting, transcript blinding, and terminal boundary reconciled without provider traffic or mutation. Constrained PowerShell hashing fell back to the pinned Python toolchain without permission broadening. C8 reverified the noninteractive discrepancy: `codex exec` reports `approval: never` and neither `exec` nor the standalone app-server exposed Browser Use. C8 therefore commissioned an explicit dedicated Edge Guest/CDP capability instead: active Guest Profile account/login/cookie residue zero, CDP bound to loopback only, zero-network `file://` specimen DOM proof, and clean `Browser.close` teardown while ordinary Edge remained running. Browser/CDP stays off by default and remains browser evidence only. C9 commissioned the independent Claude review plane through a dedicated `.claude-ensemble` configuration, claude.ai Pro subscription authentication with no API-key route, and one tool-less/no-MCP/no-session-persistence exact-SHA review. The reviewer returned `PASS_WITH_FINDINGS`; the manager adopted the material C8 origin-isolation/permission findings and repaired them rather than retrying the reviewer. Claude remains advisory. C10 Hook pilot was executed on the pinned `codex-cli 0.153.4` realization and is **blocked/falsified**: deterministic deny worked, but deliberate nonzero `PreToolUse` hook-process failure was fail-open and the underlying command executed. The ambient `0.154.0` installation was not substituted or live-tested. Post-falsification source inspection established that `rust-v0.153.4` and `rust-v0.154.0` use the same relevant `pre_tool_use.rs` Git blob (`c1baebd4f01fd1906f461fe84cb2bf0d73058bfc`) and that generic hook-process failures remain fail-open; upstream issue #41979 still requests opt-in fail-closed handling. Director disposition therefore source-disqualifies 0.154.0 from a redundant live C10 attempt. A future Codex candidate becomes eligible for C10 revalidation only after exact source/release inspection proves `PreToolUse` process-failure paths can block tool dispatch; then the exact version/hash must be commissioned and the strengthened multi-failure-class suite run. Until then Hooks remain disabled and C11+ remain blocked.

Director amendment 2026-09-11 adds C9A as an independent, on-demand, repository-read-only Claude review-orchestration gate after C9. C9A may build/hash one fixed exact-ref packet in Administrator scratch, invoke the dedicated C9 Claude plane once, capture/hash structured advisory output and runtime telemetry, and return it for manager reconciliation. It may not mutate project/Git state, schedule/background/poll/self-trigger, use Claude tools/MCP/subagents/session persistence, fall back to API-key/cloud-provider billing, use browser/CDP, invoke product-provider traffic, retry automatically, or act on findings. C9A remains an approved architecture, but no further live C9A realization is currently earned; it does not satisfy C10 or unlock C11.

C9A live commissioning on 2026-09-11 did not pass. R1's 84,199-byte and 30,565-byte packets both failed closed at 180 seconds, falsifying packet size alone as sufficient explanation. R2 then retained Claude Code 2.1.267/high effort/180-second bound and all isolation controls, removed native `--json-schema`/StructuredOutput, added `--max-turns 1`, local JSON validation, and timeout/post-snapshot telemetry. Exact-head Validation #701 passed, but the single authorized 44,452-byte R2 packet also failed closed at 180 seconds with zero result/stderr bytes; sealed telemetry proved zero retry and identical repository state before/after. PR #90 closed unmerged and the candidate is archive-tagged. C9A remains uncommissioned; any later live attempt requires a new explicit materially changed hypothesis/decision and separately validated realization.

Current commissioned model mapping (later-gate execution remains blocked until earned):

| Task class | Surface/model | Reasoning | Usage class |
|---|---|---|---|
| routine Administrator triage/routing | Codex + GPT-5.6 Sol | Medium | normal |
| normal implementation/test/browser work | Codex + GPT-5.6 Sol | High | normal |
| difficult repository/browser/runtime/computer-use work | Codex + GPT-6 Astra | High | scarce |
| exceptional critical falsification/refactor | Codex + GPT-6 Astra | highest justified available effort | scarce |

Codex is the execution environment. GPT-6 Astra is a scarce model allocation inside that environment, not a synonym for Codex.

Astra should not be spent on ordinary status checks, routine documentation edits, queue maintenance, or work GPT-5.6 Sol can execute at required quality.

## Browser Developer Mode / CDP

Browser/CDP may be used for development/browser validation only when explicitly enabled for a bounded task. C8 proved the dedicated Edge Guest/CDP path; standalone Codex `exec` and app-server Browser Use were not model-visible and must be reverified before relying on that surface. CDP can inspect browser page/DOM state, console/network behavior, and other browser-visible facts.

Security discipline:

- use the dedicated Kymaean browser area with a fresh or zero-residue Guest context;
- verify zero account/login residue before use and keep unrelated authenticated personal sessions out of that context;
- keep CDP loopback-only and directly test any permission/origin-isolation property being claimed;
- treat CDP/browser evidence as browser evidence only;
- never relabel it as WinUI/native Windows accessibility/runtime/WACK/Store validation.

This development capability does **not** reopen the product Blueprint prohibition on consumer-chat/browser automation as an Ensemble runtime dependency.

## OpenAI Developers plugin

The OpenAI Developers plugin is installed and works best in Codex.

Use it only when an OpenAI-specific development task is actually in scope, for example:

- current OpenAI API documentation/behavior;
- OpenAI API troubleshooting;
- safe OpenAI API-key setup;
- Agents SDK / ChatGPT Apps development if a separately authorized dev/eval tool genuinely requires those surfaces.

It does not authorize binding Ensemble Core/Application to OpenAI, adopting Agents SDK as product architecture, or bypassing provider abstraction/product law.

OpenAI API billing is separate from the ChatGPT subscription. Never create/use an API key merely because the plugin can do so; follow the explicit credential/billing gate for the task.

## Claude allocation — C9 commissioned; C9A architecture approved; R2 corrected attempt authorized, not consumed

Claude Code is commissioned only as an independent advisory review plane. It does not inherit Administrator, Engineering, provider, validation, product, Design, or ODR authority.

Current verified realization is Claude Code `2.1.267` on Windows ARM64, installed through WinGet and isolated under `C:\Users\Wiryl\.claude-ensemble` via `CLAUDE_CONFIG_DIR`. Authentication is `claude.ai` / first-party / Pro subscription. Keep `ANTHROPIC_API_KEY` and cloud-provider routes absent; Claude Console/API credits are separate billing and require separate explicit authority.

For repository-independent review, prefer a fixed exact-SHA packet outside the repository and disable built-in tools, inherited MCP, permission prompting, and session persistence. Do not pin a permanent Claude model name here; record the actual runtime-selected model/provider telemetry with each review. Returned Claude findings are advisory until reconciled by the owning Sol/Administrator.

C9's commissioning review used exact Project merge `e9511cacb2a58e59aa18595248bdf9ea16d7c435`, returned `PASS_WITH_FINDINGS`, and materially improved C8: cross-origin storage isolation and actual permission denial were then proved directly. Do not retry an adverse reviewer merely to obtain agreement.

C9A must reuse this isolated review posture rather than broaden it. Before C9A commissioning evidence passes, do not claim automated Claude routing exists; after it passes, only the exact on-demand advisory dispatcher is commissioned.

Current realization result: **NOT COMMISSIONED**. The two R1 attempts and the single authorized R2 attempt are immutable timeout failures and must not be retried. R2 passed its offline/exact-head gates but timed out at 180 seconds after removing native StructuredOutput; its sealed telemetry recorded zero result/stderr bytes, zero retry, and identical repository state. C9 manual review remains available.

## Dispatch requirement

Whenever the Director must invoke an external surface manually, provide this header before the complete prompt:

```text
SURFACE: <surface>
MODEL: <current recommended model>
REASONING/EFFORT: <current recommendation>
MODE: <read-only | implement | browser | execute | review | other>
USAGE CLASS: <normal | scarce>
```

The Director should not have to infer the optimal model/effort from task complexity.

## Re-verification triggers

Re-verify this snapshot when any of the following occurs:

- a named model or effort mode is added/removed/renamed;
- plan allowance/billing behavior materially changes;
- Codex Administrator configuration changes;
- Claude integration becomes active;
- Browser Developer Mode/CDP behavior or security guidance changes;
- an external dispatch depends on a capability not verified recently enough to trust.

When only this snapshot changes, do not edit frozen architecture or `CURRENT_STATE.md` unless the active engineering boundary itself changes.