# E0-A Fresh-Chat Handoff — Provider Compatibility Blocked Transition

Status: **BLOCKED TRANSITION — Q-E0A-01 CLOSED — Q-E0A-02 BLOCKED ON EXACT PROVIDER AUTHORIZATION — PROVIDER AUTHORIZATION NONE**

Recorded: 2026-09-09

Authority: temporary transition guidance only. It is live only while `CURRENT_STATE.md` names this exact path. It cannot authorize provider traffic, advance phase, change validation authority, or create product/policy authority.

## Fresh-chat first action

1. Resolve all live `Rylascoo/Ensemble-Project` refs from GitHub and read `CURRENT_STATE.md` at exact current `main` first. Repository state overrides every snapshot below.
2. Read `docs/PROJECT_AUTHORITY.md`, `docs/ENGINEERING_HYGIENE_CONSTITUTION.md`, `docs/PROJECT_EXECUTION_QUEUE.md`, and `docs/VALIDATION_LEDGER.md`.
3. Read current E0-A authority/evidence:
   - `docs/evidence/E0A_GEMINI_PROVIDER_COMPATIBILITY_DIRECTOR_DECISION_2026_09_08.md`;
   - `docs/blueprint/E0A_GEMINI_BOUNDED_PROVIDER_ERROR_DIAGNOSTIC_AMENDMENT.md`;
   - `docs/evidence/E0A_GEMINI_BOUNDED_PROVIDER_ERROR_DIAGNOSTIC_IMPLEMENTATION_AUDIT_2026_09_08.md`;
   - `docs/evidence/E0A_GEMINI_BOUNDED_PROVIDER_ERROR_DIAGNOSTIC_NATIVE_ARM64_VALIDATION_2026_09_08.md`;
   - `docs/evidence/E0A_THIRD_REAL_GEMINI_35_LITE_ATTEMPT_03_ARCHIVE_AUDIT_2026_09_08.md`;
   - `docs/evidence/E0A_GEMINI_AUTH_TRANSPORT_DIFFERENTIAL_RESULT_2026_09_08.md`;
   - `docs/evidence/E0A_GEMINI35_QUOTA_SNAPSHOT_REUSE_DIRECTOR_DECISION_2026_09_09.md`;
   - `docs/evidence/E0A_FRESH_CHAT_CONTINUITY_RECONCILIATION_2026_09_09.md`.
4. Re-resolve latest CI at current ref. Hosted compiler/test evidence must not be promoted into native Windows ARM64 runtime authority.
5. Do not infer an active source implementation branch from historical branch names.

## Current engineering boundary

- runtime phase: **E0-A Experimental Harness — Phase B**;
- Q-E0A-01: **DONE / machine-validated**;
- Q-E0A-02: **BLOCKED on exact Director provider-action authorization and applicable execution preconditions**;
- promoted native executable: `e6e7d6c8c7187a87df2c97f2373dd3adbee7ce4a`;
- validation tag: `validation/e0a-gemini-bounded-provider-error-diagnostic-native-arm64`;
- native result: Core **622/622**, Harness **130/130**, fresh build, fixture smokes and credentialless gates **PASS**;
- provider authorization: **NONE**.

There is no active source implementation package. Any run-preparation branch is documentation/authorization staging only and does not become executable authority.

## Provider evidence already closed

Attempt 03 reached the first Performer `countTokens` call and received HTTP **400** with 0 accepted turns, no generation, and $0 shadow spend. A same-key/header/model/method simple `countTokens` request succeeded, so authentication transport is closed as the failure explanation. The exact rejected field remains unproven.

The bounded provider-error diagnostic is implemented and native-validated. Do not redesign or reimplement it without new evidence.

## 2026-09-09 quota decision

The Director reconfirmed that the intended AI Studio Free-tier `gemini-3.5-flash-lite` route remains:

```text
15 RPM
250,000 input TPM
500 RPD
```

and directed the project to use these values until a material contrary signal occurs.

`docs/evidence/E0A_GEMINI35_QUOTA_SNAPSHOT_REUSE_DIRECTOR_DECISION_2026_09_09.md` supersedes only the old requirement to manually reread the same quota limits before every separately authorized 3.5 Flash-Lite run. A separate manual quota-limit read is not required while the exact route remains unchanged and no listed trigger occurs.

Reverify the quota snapshot after a provider `429` / `RESOURCE_EXHAUSTED`, an observed AI Studio limit change, project/account/tier/key/route/model change, material provider rate-limit announcement, material known competing project traffic, or credible contradictory evidence.

This does not override the executable's pricing/data-use freshness guard, model lifecycle/policy change triggers, synthetic-only Free-tier restriction, or separate provider authorization requirement.

## Current provider stop state

**3.5 Flash-Lite live execution remains PAUSED until one exact provider action is explicitly authorized. 3.1 Flash-Lite remains DEFERRED. Provider authorization is NONE.**

Do not create inference traffic, enter credentials into a provider invocation, send `countTokens`, retry Attempt 03, advance 3.1 Flash-Lite, or treat quota readiness as network authorization.

## Next exact gate

The next Director decision may authorize one bounded fourth synthetic Missing Raft `GEMINI-3.5-FLASH-LITE-MINIMAL` E0-A attempt using exact native-tested executable `e6e7d6c8...`.

If proposed, the authorization must state at minimum:

- exact executable/tag and frozen synthetic Missing Raft fixture/profile;
- exactly one run authorization;
- one attempt per role invocation, zero automatic retries, no fallback or alternate provider/model/profile traffic;
- first full-request `countTokens` failure terminates the run and only the approved bounded diagnostic is retained;
- successful `countTokens` may continue within the same authorized frozen run to generation;
- existing spend, evidence, privacy, rate-discipline, timeout, cancellation, and cleanup laws remain binding;
- any terminal result consumes the authorization.

Before execution, prefer a separate linked execution worktree pinned to the exact validated tag/checkout. Preserve the historical validated root and the native-validation worktree as immutable corroboration surfaces.

## Standing exclusions

Do not:

- consume provider traffic or spend without the exact explicit Director authorization;
- infer retry/fallback authority from a technical failure;
- patch ambiguous provider fields from documentation conflict alone when the bounded diagnostic can obtain live evidence;
- broaden the countTokens-only diagnostic contract to generation errors without a separately justified amendment;
- advance E0-B+ before E0-A closes;
- execute E0-E before E0-A -> E0-B -> E0-C -> E0-D close;
- alter Core/fixture semantics without separate architecture authority;
- convert design-lane queue activity into engineering authority;
- inflate hosted/compiler evidence into native or behavioral evidence.

Before consequential action, recursively audit correctness, consistency, authority, scope, tests, simplicity, hygiene, ARM64 suitability, privacy/security, evidence, branch topology, provider facts, and queue state until one complete pass finds no material correction or worthwhile improvement.
