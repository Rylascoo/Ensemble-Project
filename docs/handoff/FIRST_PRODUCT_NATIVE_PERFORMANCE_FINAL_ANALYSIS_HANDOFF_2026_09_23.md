# START HERE — First Product-Native Performance Final Analysis — Fresh Chat Handoff

Status: **ACTIVE CONTINUITY HANDOFF ON DOCUMENTATION BRANCH — FINAL READ-ONLY ANALYSIS NEXT**

Date: 2026-09-23

This handoff preserves the newest Director decisions and the exact stopping point after the current chat exceeded its practical length. It is a recovery locator, not authority. Fresh work must resolve live refs and read current repository authority normally.

## Recover first

Fresh-resolve all live refs for `Rylascoo/Ensemble-Project`.

Expected integrated main at handoff creation:

`f685a21a699f3c9300a8db15358aa5d5d125115f`

Documentation continuity branch:

`governance/first-product-performance-final-analysis-handoff-2026-09-23`

Read on the exact current continuity-branch head:

1. `AGENTS.md`
2. `CURRENT_STATE.md`
3. `docs/PROJECT_AUTHORITY.md`
4. `docs/ENGINEERING_HYGIENE_CONSTITUTION.md`
5. `docs/PROJECT_EXECUTION_QUEUE.md`
6. `docs/VALIDATION_LEDGER.md`
7. `docs/roadmap/KYMAEAN_ARCHITECTURE_AND_SHIP_PLAN.md`
8. `docs/KYMAEAN_PRODUCT_BUILD_AHEAD_OF_DEFERRED_E0_VALIDATION_DIRECTOR_AMENDMENT_2026_09_17.md`
9. `docs/FIRST_PRODUCT_NATIVE_PERFORMANCE_DIRECTOR_DISPOSITION_2026_09_23.md`

Do not infer current authority from this handoff if live refs differ.

## Frozen Blueprint input

Project-backed file:

`ENSEMBLE_BLUEPRINT_0.1_FROZEN_FOR_E0.txt`

Expected exact-byte SHA-256:

`36cfd75d48cf5e51c2958a4c31dd60007ad578fde02e60623464daa4a4012810`

Blueprint 0.1 defines the creative loop and constitutional boundaries. It is Project-backed input, not currently committed to `Ensemble-Project`.

## Key reconciliation already established

The earlier statement “Kymaean cannot generate anything” was corrected.

- E0/H1 already implements and has exercised the deterministic generative loop.
- E0-A Run 08 reached 12 accepted turns and sealed hard-gate PASS.
- `Ensemble.E0.Core` contains proven Access, Context, Performer Candidate/Attempt, Director Opportunity, Integrity, State Interpreter, State Authority, Take, atomic causal commit, synchronized cycle and Turn orchestration.
- Current `Kymaean.Application` does not yet own or expose that loop.
- The architectural problem is therefore how validated E0 mechanisms lawfully become provisional Product semantics without research silently becoming Product authority.

The 2026-09-17 Director amendment already allows provider-runtime abstractions, credential boundaries, streaming/cancel/retry/spend provenance and fake/test adapters. New non-experimental Gemini traffic remains held until the separate development-provider-association boundary is satisfied. Do not collapse provider-runtime implementation, Performer Product semantics and provider network traffic into one “provider gated” claim.

## Current Director decisions

Read the durable branch record:

`docs/FIRST_PRODUCT_NATIVE_PERFORMANCE_DIRECTOR_DISPOSITION_2026_09_23.md`

In short:

- deterministic test-adapter Performer counts for **first Product-native Performance**;
- **first live AI Performance** is separate/later;
- the milestone must make the committed consequence visible in the acting Character's own later **bounded Product context**, not only replay;
- first Opportunity: explicit typed `SceneId + CharacterId`, no automatic Director selection;
- first Scene targeting: explicit established `SceneId`, no Current/Active Scene semantics;
- first Take/consequence acceptance: bounded deterministic provisional policy, ODR-19 stays open;
- first Performer binding: invocation-scoped deterministic adapter, no persistent provider/model/understudy casting.

## FINAL ANALYSIS TASK — Q1 / Q2 / Q5 ONLY

This is the final architecture-analysis round before implementation.

Role: **Product / Architecture — read-only decision input**

No source mutation, provider traffic, deferred-E0 consumption, Design change, queue mutation or implementation in this task.

### Q1 — E0 → Product acquisition mode

Compare:

1. bounded direct reuse behind Product adapters;
2. extraction/generalization;
3. Product-native semantic port;
4. clean re-derivation from frozen contracts/oracles.

For every viable option return:

- exact projects/files affected;
- new Product types/interfaces required;
- whether `Kymaean.Application` gains a project reference to `Ensemble.E0.Core`;
- identity-translation cost (`CharacterId` / `SceneId` currently exist independently in Product and E0);
- which E0 experimental assumptions would cross the boundary;
- deferred-E0 exposure and reversibility;
- one recommendation labeled **PROPOSED**;
- exact falsifier for that recommendation.

Do not call one option “smallest” without comparing measured footprint.

### Q2 — minimum Character context/access

The milestone requires the first consequence to affect the acting Character's own later bounded Product context.

Known E0 evidence, not Product law:

- H1 Patch 0004 permits the requested Character's own Circumstance;
- H1 Patch 0005 includes Circumstance in context;
- Product World-current truth explicitly does not transfer/reveal/mutate Character knowledge.

Determine the smallest Product-owned context + access semantics that satisfy the milestone without silently importing the whole E0 ontology.

Return:

- exact Product types/events/projections needed;
- exact access rule;
- whether the rule is adopted from E0, adapted, or re-derived;
- what Product semantics remain excluded;
- deferred-E0 exposure;
- one recommendation labeled **PROPOSED** + falsifier.

### Q5 — first consequence domain

Evaluate the narrowest viable durable consequence family jointly with Q2.

Character Circumstance is a candidate, not an adopted decision.

Include all relevant E0 policy evidence, including `UnresolvedProposition + Add` where relevant. Distinguish E0 evidence from Product authority.

Return:

- candidate domains;
- exact Product event/projection footprint;
- how the acting Character later receives the changed value under Q2;
- atomic history shape;
- deferred-E0 exposure/migration implications;
- one recommendation labeled **PROPOSED** + falsifier.

## Known persistence boundary

Current Product persistence is append-only journal/event replay.

A later implementation that introduces durable Performance/Take/consequence history triggers:

`docs/ENGINEERING_HYGIENE_CONSTITUTION.md` deferred-validation exposure requirements.

The final analysis must explicitly cover:

- open deferred gates;
- exposure class;
- falsifier;
- replaceable seam;
- versioning;
- migration or fail-closed behavior if Product history is reshaped;
- rebuildable projection behavior.

## STOPPING RULE

This analysis must terminate the architecture-analysis loop.

After Q1/Q2/Q5 return:

1. independent Claude review may falsify the returned evidence/options;
2. Director disposes Q1/Q2/Q5;
3. the next Product artifact is a bounded **implementation package**.

Do not propose another architecture memo unless the final analysis proves one question cannot be answered without new executable evidence and identifies that exact evidence.

## Claude role

The Director has updated the ongoing Claude Opus 5.5 chat to the 2026-09-19 repository role:

- read-only advisory/falsification;
- no Codex dispatch/model authority;
- no Git/Drive mutation authority;
- no second backlog.

Claude has independently reviewed the first-Performance memo and subsequent decision package. Its latest review accepted the substance with corrections and identified the Q1/Q2/Q5 final narrow pass plus the stopping rule.

## Q-DESIGN-24 / Director Preview continuity

Do not lose this parallel state:

- Q-PREVIEW-01 integrated on main.
- Corrected Q-DESIGN-24 remains draft PR #262 at `abada447f4a6e4a8660146d641b2984bb38c5dec`, unmerged and Design-INCONCLUSIVE.
- Preview-only local composite source `dd56b30aeaa01e69ccd0435a3b680d6c1b4cd350` received **TECHNICAL PREVIEW ADMISSION PASS** on SurfSeven.
- Installed Preview version: `1.2458.82.2083`.
- Active rich evaluation profile: `614a648f101b4f4fb7ca2b7202c72fdf`.
- Separate falsifier: `0a8c4c73814e454187ec0bbd41848ce9`.
- Existing profiles were preserved byte-for-byte.
- Natural Preview use remains allowed; Q-DESIGN-24 acceptance is still withheld.
- Current Production workspace composition remains deferred pending Director disposition.
- Q-ALPHA-01 remains blocked.

Local Preview admission documentation source:
`f91b2f6fc3ee362c7daa013c198dd965e9ae0aaa`
in the preserved local Preview composite worktree; this is not Product integration authority.

## Governance/process findings to preserve for later

Not on the critical path to the final Q1/Q2/Q5 analysis:

- README repository-residency wording is stale.
- `docs/design/app/CURRENT_CONTRACTS.md` contains stale Q-DESIGN-24 “IMPLEMENTATION NOT AUTHORIZED” wording despite later empirical authorization.
- post-merge closeout PR cost has a measured structural cause: pre-merge state prose becomes stale at merge and exact-main facts only exist afterward.
- Q-ADMIN-07 remains deferred/local-residue scoped; remote merged-branch cleanup would require explicit scope amendment and archive-tag-before-delete.
- 15 merged remote branches had zero unique commits at the prior audit; preserve the two diverged branches unless separately dispositioned.
- human Preview evidence uses the premise test: Director knowledge acquired outside the interface cannot prove that the interface taught that meaning.

These are later governance/Design maintenance, not reasons to delay first Product-native Performance work.
