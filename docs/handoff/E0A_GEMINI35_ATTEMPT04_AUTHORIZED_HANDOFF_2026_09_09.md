# E0-A Fresh-Chat Handoff — Gemini 3.5 Flash-Lite Attempt 04 Authorized

Status: **AUTHORIZED TRANSITION — Q-E0A-02 ACTIVE ONLY FOR ONE EXACT ATTEMPT 04 — AUTHORIZATION NOT YET CONSUMED**

Recorded: 2026-09-09

Authority: temporary transition guidance only. It is live only while `CURRENT_STATE.md` names this exact path. It cannot enlarge the Director authorization, alter native validation authority, authorize retries/fallbacks, or advance E0-B+.

## Fresh-chat first action

1. Resolve current `Rylascoo/Ensemble-Project` refs and read `CURRENT_STATE.md` at exact current `main` first.
2. Read `docs/PROJECT_AUTHORITY.md`, `docs/ENGINEERING_HYGIENE_CONSTITUTION.md`, `docs/PROJECT_EXECUTION_QUEUE.md`, and `docs/VALIDATION_LEDGER.md`.
3. Read:
   - `docs/evidence/E0A_GEMINI35_ATTEMPT04_DIRECTOR_AUTHORIZATION_2026_09_09.md`;
   - `docs/evidence/E0A_GEMINI35_QUOTA_SNAPSHOT_REUSE_DIRECTOR_DECISION_2026_09_09.md`;
   - `docs/blueprint/E0A_GEMINI_BOUNDED_PROVIDER_ERROR_DIAGNOSTIC_AMENDMENT.md`;
   - `docs/evidence/E0A_GEMINI_BOUNDED_PROVIDER_ERROR_DIAGNOSTIC_NATIVE_ARM64_VALIDATION_2026_09_08.md`;
   - `docs/evidence/E0A_THIRD_REAL_GEMINI_35_LITE_ATTEMPT_03_ARCHIVE_AUDIT_2026_09_08.md`;
   - `docs/evidence/E0A_GEMINI_AUTH_TRANSPORT_DIFFERENTIAL_RESULT_2026_09_08.md`;
   - `docs/evidence/E0A_GEMINI35_THOUGHT_SIGNATURE_COMPATIBILITY_AUDIT.md`;
   - `docs/evidence/E0A_FRESH_CHAT_CONTINUITY_RECONCILIATION_2026_09_09.md`.
4. Re-resolve latest CI at the current docs head. Hosted/compiler evidence does not replace native Windows ARM64 authority at `e6e7d6c8...`.
5. Before any provider action, verify the authorization record is still **AUTHORIZED — NOT YET CONSUMED** and no material quota/provider trigger has occurred.

## Exact executable authority

- executable checkout: `e6e7d6c8c7187a87df2c97f2373dd3adbee7ce4a`;
- validation tag: `validation/e0a-gemini-bounded-provider-error-diagnostic-native-arm64`;
- native result: Core **622/622**, Harness **130/130**, build/smokes/credentialless PASS;
- fixture: canonical synthetic Missing Raft only;
- profile: `GEMINI-3.5-FLASH-LITE-MINIMAL` / `CREATIVE-MINIMAL`;
- run id: `E0A-REAL-G35L-20260909-04`.

No later documentation commit inherits or replaces that native executable identity.

## Exact live authorization

The Director authorized exactly one fourth real synthetic Missing Raft E0-A attempt at that executable/tag with:

- one attempt per role invocation;
- zero automatic retries;
- no fallback;
- no alternate provider/model/profile/fixture;
- no standalone compatibility/authentication probe or unrelated provider traffic;
- existing rate, spend, privacy, evidence, timeout, cancellation, and cleanup laws unchanged.

If the first full-request Performer `countTokens` fails, the run terminates immediately and no generation is authorized. Only the approved bounded diagnostic may persist. If it succeeds, the same authorized run may continue through the frozen generation/role path.

A local preflight failure before provider invocation does not consume the authorization. Once the first provider-bound invocation begins, any terminal result consumes it and creates no retry authority.

## Local preflight 01 — non-consuming packet defect

The first local Attempt-04 packet invocation on 2026-09-09 created the dedicated linked worktree:

`C:\Users\Wiryl\Sol Dev\Ensemble-Project-Worktrees\e0a-gemini35-attempt04`

at exact detached checkout `e6e7d6c8c7187a87df2c97f2373dd3adbee7ce4a`, then failed before build/credential entry/provider traffic because its Windows PowerShell StrictMode host guard referenced `RuntimeInformation.OSArchitecture`, which was unavailable in that shell/runtime surface.

The packet reported `ATTEMPT04_AUTHORIZATION=NOT_CONSUMED` and `PROVIDER_TRAFFIC_NOT_STARTED=YES`. No API key was requested or activated, no evidence root was created, no provider invocation began, and no spend occurred. The defect is confined to the PowerShell packet, not the native executable or frozen request.

The corrected packet must **reuse the already-created Attempt-04 execution worktree**, after exact HEAD/detached/clean/tag/material-untracked checks. Do not create a second execution worktree merely because preflight 01 failed.

## Execution topology

Use the existing Attempt-04 linked Windows worktree pinned to the validation tag/checkout. Do not repurpose either:

- historical validated root `C:\Users\Wiryl\Sol Dev\Ensemble-Project`;
- existing native-validation worktree for `e6e7...`.

Before provider contact, fail closed on any of:

- missing execution worktree, tag/check-out mismatch or non-detached execution checkout;
- dirty tracked/staged worktree or material untracked source/test/fixture content;
- non-native ARM64 .NET host/runtime mismatch;
- failed fresh Harness build;
- canonical Missing Raft fixture mismatch;
- nonfresh/colliding evidence root;
- stale pricing/data-use guard;
- quota/provider material-change trigger;
- missing or mismatched local credential entry;
- any attempted alternate profile, retry, fallback, or extra provider request.

The packet should use shell-neutral Windows checks plus `dotnet --info` and the native Harness's own ARM64 guard rather than relying on the unavailable PowerShell `RuntimeInformation.OSArchitecture` member.

## Consumption / terminal handling

Emit `PROVIDER_INVOCATION_STARTED=YES` immediately before launching the single live-host invocation. That event consumes the one-run authorization.

After the process terminates:

1. remove provider credentials from the execution environment;
2. preserve/hash only the allowed evidence package;
3. do not relaunch under this authorization;
4. update the authorization record to CONSUMED and provider authorization to NONE;
5. record and recursively audit the terminal evidence before any source/provider decision.

## Standing exclusions

Do not:

- infer retry/fallback authority from any failure;
- patch ambiguous Gemini request fields before collecting the authorized live diagnostic;
- broaden the countTokens-only diagnostic to generation failures without a new amendment;
- run 3.1 Flash-Lite or another provider/profile;
- alter Core/fixture semantics;
- advance E0-B+ before E0-A closes;
- execute E0-E before E0-A -> E0-B -> E0-C -> E0-D close;
- treat Design Sol activity as engineering/provider authority.

Before consequential action, recursively audit correctness, consistency, authority, scope, privacy/security, evidence, ARM64 suitability, branch topology, provider facts, and queue state until one complete pass finds no material correction or worthwhile improvement.
