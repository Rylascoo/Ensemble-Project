# Codex Administrator C7 - Real local-evidence pilot

Status: **C7 MECHANICAL PASS after R1 owning-manager recursive audit; durable Project adoption requires this closeout branch to merge.**
Date: 2026-09-10

## Scope and authority

C7 implements only the Director-approved Runtime Specification real local-evidence gate. It creates no Engineering, Design, product/ODR, provider/spend, validation, browser/CDP, Claude, Hook, Automation, or C8+ authority.

Authoritative Project baseline at dispatch and manager adoption start: `main@8e486567e8d3f43f01c065a083f3fa1f9ba724e9`.

Exact C7 contract: use preserved Q-E0A-03 terminal evidence as the preferred read-only specimen only when it does not delay or replace active Engineering. Pass requires local hash/evidence analysis, no provider call, no source correction, no validation-rung promotion, no authority mutation, correct failure classification, and a useful bounded return to Engineering Sol.

During preflight, active Run-04 evidence and an Engineering worktree for `q-e0a-03-run04-control-id-alignment-2026-09-10` were detected. C7 treated them as active Engineering and explicitly prohibited any Run-04 inspection or mutation.

## Commissioned runtime

Runtime home: `C:\Users\Wiryl\.codex-ensemble`.

Codex: `codex-cli 0.153.4`; executable SHA-256 `77F792476FE0DEF726503F02A7C55F485E562DD7AD8801FE61DC8F4BF9991D20`.

Administrator profile: `gpt-5.6-sol`, reasoning `medium`, read-only sandbox. The corrected noninteractive execution reported `approval: never`, despite the commissioned launcher carrying the on-request intent. Because C7 required no approval-gated action, this did not broaden permission or invalidate the read-only proof; it is a mandatory re-verification item before C8 depends on approval behavior.

The optional GitHub connector returned OAuth `AuthRequired` at startup. Fresh manager-side fetch had already established local `origin/main` at the exact dispatched authority ref, and the Administrator recovered repository law with immutable `git show`/`git cat-file` reads. Connector authentication therefore was not used as authority and did not block this local-evidence gate; it must be reverified before a later gate depends on that connector.
## Invocation and failure discipline

The first manager invocation remained blocked waiting for stdin because the desktop process kept stdin open while a prompt argument was supplied. It was classified `TOOL_FAILURE`, terminated without repository/evidence mutation, and corrected once by explicitly closing redirected stdin.

Inside the corrected read-only run, the historical root checkout was detected as detached at `689655eed677b789ab3ee395f1c65b4f2cb72cc8`. It was not treated as authority and was not changed. Exact-ref authority was recovered from local `origin/main@8e486567e8d3f43f01c065a083f3fa1f9ba724e9`.

One PowerShell verification command failed at parse time and one Python `-c` invocation failed from native-argument quoting; both were classified `TOOL_FAILURE` and produced no evidence mutation. A corrected PowerShell hashing attempt then encountered the commissioned host's constrained-language restriction on byte/hash APIs; its derived hash/seal fields were discarded and the restriction was classified `ENVIRONMENT_FAILURE`.

The unresolved deterministic checks were completed with the already-admitted pinned ARM64 Python 3.12.10 toolchain over stdin, without permission broadening or a repeat of the blocked PowerShell mechanism.

## Sealed specimen

Specimen: `C:\Users\Wiryl\Sol Dev\Ensemble-Project-Evidence\E0A-Q03-G35L-20260910-03`.

Direct census: 63 files, 380,447 bytes. `manifest.json.runId`, directory basename, and all 21 attempt-ID prefixes resolve to `E0A-Q03-G35L-20260910-03`.

Required SHA-256 results:

| Artifact | SHA-256 |
|---|---|
| `manifest.json` | `47027f8a5f799ff9c62568466153c64c257b53f35c58ef8d322834cb9985bf77` |
| `run.final.json` | `c87cc695fd6d4f32932dd9be2c868922616c3200e9aeade98bb003815af190d3` |
| `run.summary.json` | `20f0f45f8f2755067e3d758b9f4476d58a2ed1b6c8b149408950d487fc4ba415` |
| `transcript.json` | `474f435b4084925754f02035d339e2b050d4394512ccd950a429b8c6c2ba2137` |
| `blind/mapping.json` | `23c6ba69a36ec3f8d91ff044bb940bc8e510dd6ef4826af3e57dcdb4c6fbca54` |
| `blind/transcript.json` | `b98b209c791c1707b7a2039f3e5938f896422b07ac5637ba120c7afd16f13897` |
| `events.ndjson` | `7ec9f6d14ec3f645b046da2749f3ed205e930d8c31296dcda3c03d8111029d34` |
| final Interpreter `terminal.json` | `c04e2dd336048f3d174750d9890be5dc9da508a910c25b8a7eae77dfbfe65921` |
| final Interpreter `request.json` | `15cbe654f3b3a38bb52a3c19c6c3a3b7e0e45c93f6c67b8b4f716f208724565f` |
| final Interpreter `stream.ndjson` | `9422cc047c5c6f30fb41bc15293287adcd97fbe52eaa1cc18deeb2769cd1bd96` |
All 62 artifacts recorded by `run.final.json` matched exact path order and SHA-256 with no duplicate, missing, extra, or unsafe recorded path. Every JSON/NDJSON artifact parsed successfully.

Recomputed runtime root: `917254de3bc1229c1996a53c495e886e2a735ec898cbfba9adba5c51695f563a` — exact match.

Recomputed runtime seal identity: `58093b2f4c653336c647b7eea1c5f4179dcdc20b9c010943aaeb146b998715b5` — exact match.

A final monitored hash pass recorded zero size, creation-time, last-write-time, or last-access-time changes.

## Runtime-result consistency

The sealed evidence directly proves terminal `InvalidOutput` with six accepted turns. Source and blinded transcripts both contain turns 1-6 exactly, and the three-row blind mapping reproduces every transcript record without mismatch.

Events contain seven Performer candidates, seven Integrity evaluations, six Interpreter proposals, six authority evaluations, and six committed turns. Twenty-one attempt terminals report provider success: seven Performer, seven Integrity, seven Interpreter.

Twenty-one `preflight.input-tokens` events and 21 `provider.completed` events carry the same 21 attempt IDs, supporting the Project record's 42-request accounting. Terminal usage sums are 36,222 input tokens, 6,121 output tokens, 4,811 reasoning tokens, zero cached-input tokens, and zero cache-write tokens.

The final Interpreter attempt completed successfully with returned model `gemini-3.5-flash-lite`, response ID `n0Wjav6CGNztz7IP-6-YiAg`, and stream finish reason `STOP`. Reassembled stream output exactly matches the terminal structured output and recorded hash `f70c66dc49a6de65fad8308c23b1b319b6f2b91a77cb619be3b40148bf88062a`.

The returned JSON satisfies the request's declared schema shape but proposes `characterGoal/add` with `subjectCharacterId=VOSS` and `targetCharacterId=WREN`. Exact executable source `cef3fc15e31192a48aa3bddd99450b65a58bd8f1` routes `CharacterGoal` through `BuildMutableCharacterMutation`, which requires `targetCharacterId` to be null. The deterministic source exception matches the Project terminal analysis. This localizes the preserved Run-03 terminal as an application semantic-contract failure after provider-successful structured output, not a provider transport/model/rate failure.

## R1 owning-manager audit

The owning manager independently recomputed all ten required hashes, the full 62-artifact path/hash set, runtime root, runtime seal, request counts, commit count, and terminal boundary from the same read-only specimen. Results matched the Administrator return exactly. Root checkout remained clean/detached and shared-Git lock residue remained zero.
## Classification correction and limits

Preserved Run-03 terminal classification: `TASK_FAILURE` — provider-successful, schema-shaped Interpreter output violated the deterministic application semantic contract.

C7 required-scope integrity mismatches: **NONE**.

Provider-global billing state and provider-global request logs were not independently established from the local specimen; they are outside the local-evidence proof and are not used to pass C7. Likewise, the Project terminal-analysis record's preserved ZIP hash, captured-stdout hash, and zero-secret-hit scan are outside the designated C7 specimen and were **not independently revalidated here**. Their absence from this specimen is not classified as `EVIDENCE_FAILURE`.

Observed execution defects were bounded `TOOL_FAILURE` / `ENVIRONMENT_FAILURE` incidents described above. Each failed before producing an adopted evidence result, caused no mutation, and was resolved through one corrected or materially different admitted deterministic mechanism rather than a blind retry loop.

## Recursive audit

- exact authority recovery: PASS;
- sealed Run-03 identity/hash/root/seal integrity: PASS;
- local request/usage/turn accounting consistency: PASS;
- terminal semantic boundary localization: PASS;
- active Run-04 isolation: PASS;
- provider traffic/spend action: none;
- source correction: none;
- validation-rung promotion: none;
- Project/Design/ODR authority mutation during pilot: none;
- browser/CDP, Claude, Hooks, Automations, C8+ activation: none;
- evidence metadata mutation: none;
- remaining C7 material inconsistency: none.

## C7 disposition

C7 PASS after R1 owning-manager audit. The commissioned Administrator demonstrated useful bounded analysis of real preserved local evidence without creating authority or interfering with active Engineering.

After this closeout is merged, post-merge CI is green, the candidate is archived, temporary branch/worktree state is safely retired, and Administrator continuity is updated, C8 Browser/CDP pilot becomes the sole next earned Administrator gate. C8 must first reverify the observed noninteractive approval-mode and GitHub-connector authentication behavior before depending on those capabilities.