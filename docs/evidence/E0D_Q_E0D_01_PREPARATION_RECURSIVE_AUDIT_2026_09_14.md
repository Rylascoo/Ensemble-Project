# E0-D Q-E0D-01 Preparation Recursive Audit

Date: 2026-09-14

Status: **PREPARATION COMPLETE — METHOD/PREREGISTRATION FROZEN — SINGLE-VARIABLE ISOLATION AUDITED — IMPLEMENTATION/NATIVE VALIDATION/LIVE EXECUTION NOT AUTHORIZED — PROVIDER TRAFFIC ZERO**

## Scope and authority

Exact preparation baseline: `main@b3cba2208b4a34fd59767ccc6efe6b4df87d0b00` after Director-authorized E0-C closure and exact-main Validation #800 PASS.

This audit closes only the Q-E0D-01 method/preregistration preparation objective named by `CURRENT_STATE.md`. It audits:

- `docs/blueprint/E0D_ABLATION_CONTROLS_METHOD_AND_ISOLATION_CONTRACT_01.md`;
- `docs/evidence/E0D_Q_E0D_01_PREREGISTRATION_2026_09_14.json`.

Authority checked: frozen Blueprint 0.1 E0-D/measurement law; Run 08 selected-reference descriptor; E0-C closure/dispersion result; Missing Raft relationship contract; deterministic Access Control/Context Composer/Director Opportunity contracts; Project authority/queue/hygiene/orchestration law.

No E0-D source implementation, RunId, namespace/evidence-root claim, credential access, provider request/probe, inference, spend, or transcript scoring occurred.

## Frozen method result

E0-D is preregistered as three fresh matched pairs / six fresh slots, one fresh Full-Ensemble control paired with each required single-variable ablation:

1. Full vs relationship-context omission.
2. Omniscient-context ablation vs Full.
3. Full vs deterministic round-robin Director routing.

Fresh matched controls are necessary because E0-C already established meaningful descriptive stochastic dispersion inside the Full reference condition. Historical Run 08 and E0-C Slot 1 remain calibration/context evidence, not E0-D scoring arms. No statistical-significance or weighted-master-score claim is authorized.

All six future slots must use one exact future native-validated E0-D executable and the Run 08 provider/model/profile/fixture/runtime settings except for the one named ablation. No tuning between slots is permitted.

## Isolation audit

### Relationship omission

The sole changed variable is relationship-context inclusion. The variant removes exactly `MissingRaftContract.RelationshipContextRecordIds` from otherwise safe Character-bounded context. Source verifies this is the derived union of `RH-001`/`RH-002`, six `REL-*` records, and five relationship-derived memories: 13 canonical records. Fixture truth, version, provenance, other context categories, history, and opportunity are unchanged.

### Omniscient context

The sole changed variable is information asymmetry. The variant remains outside safe Access Control/reference Composer. At the same Production state it evaluates the three normal safe `CharacterAccessProjection`s, discards the audit `Decisions` surfaces, and builds a category-preserving ordinal union by canonical RecordId. Subject identity, roster/schema, recent Performance history, and opportunity remain unchanged. Denied IDs, authoritative provenance, chronology, credential/provider/technical data remain excluded.

Because this frozen ablation intentionally removes the reference access boundary, normal hard-gate items that prohibit cross-Character inaccessible information cannot be used to fail the omniscient arm for doing exactly what the ablation requires. Only for this variant they are replaced by preregistered conformance checks for exact union membership/completeness, category preservation, no technical/provenance leakage, and explicit ablation provenance. All other hard gates remain active. No product/debug bypass is created.

### Round-robin

The sole changed variable is Director opportunity-selection strategy. Fixture genesis remains `initialOpportunity=VOSS`; canonical roster is `MARLOWE,VOSS,WREN`. After each accepted Performance the experimental strategy advances to the next roster member, yielding the frozen 12-Turn subject schedule `VOSS,WREN,MARLOWE` repeated four times. The strategy shares the reference Director input/proposal semantic shapes while nomination/address metadata cannot alter selection.

## Measurement and execution-law audit

Each eligible pair uses the ten frozen E0 experiential dimensions plus one preregistered mechanism question. LEFT/RIGHT mapping is created only after pair eligibility, committed before scoring, and withheld until score seal. The contract accurately calls this **arm-identity blinding** only; transcript content may make a condition inferable.

Each claimed slot gets one launch maximum. There is no retry/replacement/seventh slot. A noncontributing slot removes only its pair from experiential scoring and does not cancel later preregistered slots. Pair/slot order and all pair UTC windows must be frozen before the first E0-D namespace claim. A fresh execution-sensitive capacity observation is required before every slot; stale evidence plus arithmetic is insufficient.

## Recursive audit history

Pass 1 found two corrections: explicitly prevent a failed slot from outcome-dependently cancelling later slots, and narrow the blinding claim from condition-blind to arm-identity-blind.

Pass 2 found one correction: replace a vague omniscient “all context” rule with the exact category-preserving union of the three normal safe Access projections.

Pass 3 checked the corrected method against source-defined relationship sets, fixture roster/genesis opportunity, Access/Composer boundaries, and Director input/proposal law. No further material error, authority conflict, second-variable change, or worthwhile in-scope simplification was found.

## Boundary and successor

Q-E0D-01 preparation is complete. The experiment itself is not executable. The next consequential gate is separate authorization for experiment-only E0-D implementation. Any implementation must prove deterministic single-variable isolation and preserve the Full reference path, then pass the applicable repository/compiler tests and native Windows ARM64 validation before a later activation can bind RunIds/windows/provider facts.

Live provider activation remains a separate later Director/provider gate. Provider traffic during this preparation was **ZERO**.
