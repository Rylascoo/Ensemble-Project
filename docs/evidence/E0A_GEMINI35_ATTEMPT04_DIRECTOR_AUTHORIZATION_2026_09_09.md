# E0-A Fourth Real Gemini 3.5 Flash-Lite Attempt 04 — Director Authorization

Status: **AUTHORIZED — NOT YET CONSUMED — EXACTLY ONE FOURTH REAL RUN**

Date: **2026-09-09**

## Director authorization

The Director stated:

> I authorize exactly one fourth real synthetic Missing Raft `GEMINI-3.5-FLASH-LITE-MINIMAL` E0-A reference attempt using native-validated executable `e6e7d6c8c7187a87df2c97f2373dd3adbee7ce4a` and its validation tag. Use one attempt per role invocation, zero automatic retries, no fallback, and no alternate model/profile/fixture or unrelated provider traffic. If the first full-request `countTokens` fails, terminate the run and preserve only the approved bounded diagnostic. If `countTokens` succeeds, the same authorized run may continue through its frozen generation path. Existing rate, spend, privacy, evidence, timeout, cancellation, and cleanup laws remain binding. Any terminal result consumes this authorization.

## Exact scope

This authorization permits exactly one invocation of the E0-A live host using:

- executable checkout: `e6e7d6c8c7187a87df2c97f2373dd3adbee7ce4a`;
- validation tag: `validation/e0a-gemini-bounded-provider-error-diagnostic-native-arm64`;
- fixture: canonical synthetic Missing Raft fixture only;
- arm/profile: `CREATIVE-MINIMAL` / `GEMINI-3.5-FLASH-LITE-MINIMAL`;
- run id: `E0A-REAL-G35L-20260909-04`;
- one attempt per role invocation and zero automatic retries;
- no fallback, alternate provider/model/profile, alternate fixture, authentication probe, standalone compatibility probe, or unrelated provider traffic;
- existing RPM/TPM discipline, 300-second role timeout, $5 shadow-spend ceiling, immutable evidence, privacy, cancellation, and cleanup laws.

The API key is entered only on the Director's local machine for this execution. It must not be committed, pasted into project evidence, or persisted after cleanup.

## CountTokens / generation boundary

The first provider-bound operation is the frozen full-request Performer `countTokens` preflight.

- If that `countTokens` request fails, the run terminates immediately. No generation or later role request is authorized after that failure. Only the already-approved bounded diagnostic contract may enter durable evidence.
- If that `countTokens` request succeeds, the same already-authorized run may continue through the frozen generation path and later frozen role sequence subject to every existing hard gate.
- A provider-side terminal result, including HTTP error, rate/quota failure, malformed/invalid response, timeout, cancellation after provider contact, technical failure, or completed run, consumes this authorization and creates no retry authority.

## Consumption boundary

A purely local preflight failure before credential activation/provider invocation — for example worktree discovery, exact-SHA/tag mismatch, dirty checkout, build failure, fixture mismatch, evidence-root collision, or credential-entry mismatch — does **not** consume the authorization because no provider action occurred.

The authorization is consumed when the packet begins the first provider-bound invocation (`PROVIDER_INVOCATION_STARTED=YES`). From that point forward, any terminal result consumes the authorization. No second provider invocation package may be launched under this record after the authorized run terminates.

## Preconditions established before execution

At authorization time:

- exact executable `e6e7d6c8...` is the promoted native Windows ARM64 checkpoint;
- validation tag `validation/e0a-gemini-bounded-provider-error-diagnostic-native-arm64` dereferences to that exact executable;
- native validation there is Core 622/622, Harness 130/130, fresh build/smokes/credentialless gates PASS;
- the bounded `countTokens` provider-error diagnostic is native-validated and preserves only approved structured status/field data;
- Attempt 03 established full-request first-Performer `countTokens` HTTP 400 with zero turns/no generation/$0, while same-key/header/model/method simple `countTokens` succeeded, closing auth transport as the explanation;
- the Director reconfirmed the current `gemini-3.5-flash-lite` route at 15 RPM / 250,000 input TPM / 500 RPD and authorized reuse until a material contrary signal under `E0A_GEMINI35_QUOTA_SNAPSHOT_REUSE_DIRECTOR_DECISION_2026_09_09.md`;
- the executable pricing/data-use snapshot remains valid through 2026-09-14 and remains independently fail-closed;
- the Free-tier run remains synthetic Missing Raft only; no private user content is authorized.

## Local preflight history

Attempt-04 local preflight 01 on 2026-09-09 created the dedicated linked execution worktree at:

`C:\Users\Wiryl\Sol Dev\Ensemble-Project-Worktrees\e0a-gemini35-attempt04`

The worktree was created detached at exact checkout `e6e7d6c8c7187a87df2c97f2373dd3adbee7ce4a` from the annotated validation tag. The packet then failed during the local host-architecture guard because Windows PowerShell under StrictMode could not resolve the static `RuntimeInformation.OSArchitecture` member used by the packet.

The packet explicitly reported:

```text
ATTEMPT04_AUTHORIZATION=NOT_CONSUMED
PROVIDER_TRAFFIC_NOT_STARTED=YES
```

No API key was requested or activated, no provider-bound invocation began, no evidence root was created, and no provider traffic or spend occurred. This was a packet-only preflight defect; it does not alter the validated executable or the frozen provider request.

Authorization therefore remains **AUTHORIZED — NOT YET CONSUMED**. The already-created Attempt-04 worktree is the canonical execution worktree for the corrected preflight. It may be reused only after re-verifying exact HEAD, detached state, clean tracked/staged/material-untracked state, tag identity, fresh evidence boundary, and all other pre-provider guards. Do not create a second Attempt-04 execution worktree merely because local preflight 01 failed.

## Execution topology

Execution uses the **separate linked Windows worktree** already created for Attempt 04 and pinned to the exact validation tag/checkout. Do not repurpose:

- the historical validated root `C:\Users\Wiryl\Sol Dev\Ensemble-Project`;
- the existing native-validation worktree for `e6e7...`.

The Attempt-04 execution worktree is disposable run residue. The historical/native-validation surfaces remain immutable corroboration points.

## Explicit exclusions

This record does not authorize:

- any source/configuration redesign or request-shape patch before the run;
- a retry after provider contact;
- any 3.1 Flash-Lite or 2.5 route;
- any separate `countTokens` diagnostic/probe outside the frozen run;
- generation after a failed first full-request `countTokens`;
- broadening the countTokens-only diagnostic to generation errors;
- alternate fixture/model/profile/provider traffic;
- E0-B or later experiment execution;
- production-provider admission.

## Post-run requirement

Immediately after the authorized run terminates:

1. clear provider credentials from the process/user environment used by the packet;
2. preserve/hash the allowed evidence package without persisting provider secrets or raw ineligible provider error prose;
3. record the terminal result and mark this authorization **CONSUMED**;
4. return provider authorization to **NONE**;
5. recursively audit the result before any new provider or source decision.

No provider traffic has occurred under this authorization at the time of this record.