# E0-B Q-E0B-01 — Preparation Audit and Director Input

Date: 2026-09-12

Status: **NON-PROVIDER PREPARATION COMPLETE — PROPOSAL RECURSIVELY AUDITED — DIRECTOR CAST/METHOD DECISION REQUIRED — NO IMPLEMENTATION OR PROVIDER TRAFFIC**

## Authority recovered

Authoritative `main` at preparation start: `a3288885c49c5d0fa28a2a4da665f5e182c30f3d`.

`CURRENT_STATE.md` records Q-E0A-03 DONE, Q-E0B-01 ACTIVE, and provider traffic blocked pending separate E0-B activation/authority.

Selected reference: `docs/evidence/E0A_Q_E0A_03_RUN08_SELECTED_REFERENCE_DESCRIPTOR_2026_09_11.json`.

Prepared method proposal: `docs/blueprint/E0B_MIXED_MODEL_CAST_METHOD_PROPOSAL_01.md`.

Frozen Blueprint 0.1 requires E0-B to assign different supported providers/models to the same three unchanged Characters and Fixture, hold assignments fixed within the batch, and compare against E0-A. All other feasible variables remain matched.

## Source-surface audit

The existing E0-A Harness cannot represent mixed casting without amendment:

- `E0ARunEnvelope.Validate()` requires one provider/model/tier across Performer, Integrity, and Interpreter;
- one `envelope.Performer` profile is used for every Character;
- the manifest records one run-level `providerProfileId` and one three-role profile set;
- the host constructs one `E0ASmoothGeminiRateDiscipline` from `envelope.ModelProfile`;
- one spend ledger is initialized from one profile's pricing/input limit;
- the run driver tracks a single `_observedModel` for every successful role receipt.

A direct model-flag swap would therefore make provenance, rate accounting, spend accounting, and model-identity assertions false. E0-B requires a bounded Harness/evidence amendment before execution.

## Candidate-route audit

The smallest already-supported mixed condition is 3.5 Flash-Lite plus 3.1 Flash-Lite within Gemini. Both profiles are live-selectable in the current catalog, use provider-native `minimal` creative control, support the 12-Turn cap, and have prior live transport/structured-output evidence.

The 2.5 Flash-Lite profile is rejected from this proposal because its frozen executable envelope is 20 RPD / 3 accepted Turns, its thinking control is budget-based rather than level-based, and Run 09 terminated at first Performer `countTokens` with HTTP 404 / `NOT_FOUND`. Using it would confound Performer identity with capacity, reasoning-control, and unresolved compatibility differences.

The recommended recast is `VOSS` -> 3.1 Flash-Lite Minimal while `MARLOWE` and `WREN` stay on the selected Run 08 3.5 reference Performer. The Fixture's initial Opportunity is exactly `VOSS`, so the alternate route is exercised on Turn 1 without changing Director law or relying on later routing.

Integrity and Interpreter remain exactly on the Run 08 3.5 reference route. Recasting those roles would change the safety/interpretation apparatus and make the E0-B variable broader than Performer diversity.

## Falsification checks

The recommendation would be falsified before implementation if any of the following is true:

1. frozen Blueprint law requires every Character to use a distinct provider/model rather than merely a mixed cast;
2. the sealed Run 08 descriptor cannot supply the matched reference variables needed by E0-B;
3. current Harness seams cannot support per-Character Performer routing without Core changes;
4. route-specific quota, model-identity, and spend accounting plus conservative shared-project request pacing cannot be made exact under one deterministic run ceiling;
5. the 3.1 route cannot preserve the semantic `LeastDeliberativeSupported` intent used by the reference creative roles;
6. another already-admitted route produces a materially cleaner single-variable condition without adding policy/transport/confound scope.

No falsifier was found in the current repository/source audit. This is architecture-preparation evidence only; provider lifecycle/account/quota facts remain volatile and are not re-established by this audit.

## Recursive audit result

The proposal was rechecked against the selected reference descriptor, frozen E0 ordering/control law, current Harness model catalog, run-envelope validation, host construction, evidence manifest, rate discipline, spend accounting, returned-model invariant, Missing Raft Fixture, and project authority boundaries.

Audit corrections incorporated before this record was frozen:

- narrowed the condition from a broad multi-route recast to one alternate Performer so mixed casting changes the fewest variables;
- kept Integrity and Interpreter on the selected Run 08 route rather than making all probabilistic roles mixed;
- rejected 2.5 Flash-Lite from the proposed cast because its 3-Turn/RPD/thinking-control/live-compatibility differences would add confounds;
- required route-specific TPM/RPD accounting and per-attempt pricing while retaining one conservative shared-project aggregate request discipline, so mixed routing cannot gain unproven quota concurrency or pacing headroom;
- required per-route returned-model invariants instead of the existing whole-run `_observedModel` rule;
- required the complete Character -> Performer map in immutable evidence rather than deriving casting from later receipts;
- froze the comparison-method requirement before transcript production by reusing the exact ten-dimension blind E0 experiential rubric already frozen for E0-E, with neutral LEFT/RIGHT identities, sealed per-dimension scoring before unblinding, and no pre-unblind weighted master score;
- preserved Core as unchanged and converted any unavoidable Core dependency into a stop/review condition.

The final pass found no further material error, authority conflation, experiment-isolation defect, provenance gap, quota/accounting ambiguity, post-hoc evaluation risk, or worthwhile scope reduction.

## Decision gate

The Director owns the exact E0-B provider/model pin and may approve, modify, or reject `E0B-MIXED-CAST-01`.

Recommended decision:

> Approve `VOSS` Performer = Gemini 3.1 Flash-Lite Minimal; `MARLOWE` and `WREN` Performers = selected Run 08 Gemini 3.5 Flash-Lite Minimal; Integrity = Run 08 3.5 High; Interpreter = Run 08 3.5 Minimal; all remaining feasible variables matched to Run 08; preserve aggregate project pacing; freeze and use the blind E0 scoring rubric before any E0-B transcript.

Until that decision is durable, no E0-B source implementation, credential access, provider request, inference, or spend is authorized by this preparation record.
