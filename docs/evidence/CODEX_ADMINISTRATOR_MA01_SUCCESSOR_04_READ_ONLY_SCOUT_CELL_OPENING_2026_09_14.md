# Codex Administrator MA-01 Successor-04 Read-Only Scout Cell Opening

Date: 2026-09-14

Status: **DIRECTOR OPENING — SEALED MA01-20260914-04 — EXACTLY ONE SINGLE-USE READ-ONLY TWO-SCOUT RUN AUTHORIZED AFTER MERGE + EXACT-MAIN VALIDATION + IRREVERSIBLE PREFLIGHT — NATIVE SUBAGENTS DEFAULT OFF**

## Director authority and scope

On 2026-09-14 the Director explicitly authorized a new MA-01 opening for sealed candidate `MA01-20260914-04`, bound to preparation manifest SHA-256 `4C7F1C47433C33D907869C90A906F07EE2BCD05C421C8943ADA5379296CBF7D4`. The authorization permits promotion of this opening through exact-head and exact-main Project validation and, only after every opening/interlock/preflight gate passes, exactly one single-use live execution with exactly two read-only Scout subagents.

Fail-closed/no-retry law remains mandatory. Git mutation, project-provider activity, and tool/command/app/MCP network activity, Hooks, Automations, Reviewer/Worker children, MA-02+, and any broader multi-agent permission are not authorized. Codex's own authenticated model transport is the execution substrate; it grants no command-network or project-provider authority. The commissioned runtime must remain native-subagent/default-multi-agent off outside this bounded CLI-override run.

Exact Project baseline: `main@36b9c4c2644d6d47c957e7910d0c609e260b5432`.
Exact Ryladmin Runtime + Continuity baseline: `main@5975f1403984786f9267471d1c7b9506e56122f4`.
Exact Website baseline: `main@4bb91292171e0d503d3a4e058ee421436221a793`.
Exact Codex realization: `codex-cli 0.154.0-alpha.6.2`, SHA-256 `21AE7DF1EF034C6522DB6EFA2B127C0073FD33EDEA6FD6EC3063EF9AF1BB94EA`.
Exact Scout profile SHA-256: `7E072EA63A29F7F09226660A7E110F428060DBB88ACC58030E85953D823892A4`.

The original MA-01 and first successor are permanently consumed/terminal. The first successor positively proved native two-Scout spawn/concurrency/read-only inheritance but failed closed on root-result corruption and installed Cloudflare MCP initialization. Local MA01-03 drafts never became Project authority and were retired before this opening; they create no execution right.

## Sealed successor-04 preparation

Scratch root: `C:\Users\Wiryl\.codex-ensemble\scratch\MA01-20260914-04`.
Preparation manifest SHA-256: `4C7F1C47433C33D907869C90A906F07EE2BCD05C421C8943ADA5379296CBF7D4`.
Preparation evidence SHA-256: `1742B6AE3385D99F46077A57F3E140F7C5DC31EEF73F9602BFF5110A0AFE8892`.
Fixture manifest SHA-256: `309D2F75A009E8A0CC0AE058A97A7515F421FE49005CAF76644D63E212F4398A`.

Sealed execution artifacts:

- `ma01-runtime-args.ps1` SHA-256 `F6F9D6F3578FEC9EF5845EF13D2345F07BE082A3D51F0296F867E0C55F948605`;
- `ma01-run.ps1` SHA-256 `1C36EAA5CAC6C5FC800F5EF612B92438980AAFCFCD3F5E5F2162F827D24A9FD6`;
- `ma01-supervisor.ps1` SHA-256 `B8F2F91A903B60296358F5BA7170BAD8657F01EB6E8E3E7F58332D2502A03AEA`;
- `ma01-kernel-job.cs` SHA-256 `68C5B439AEB22326E76D0D92EE222256CED130930EF43EF4FF0C24D6A5FA5EE7`;
- `ma01-adjudicator.py` SHA-256 `227B5833FD1B2E2688C7DE489478CA54BA85BE7396C89CDDC05F4FB39A77A5D0`;
- `ma01-config-preflight.ps1` SHA-256 `6A0167BF7807E24C194EBC3CD70D1461A97B3588E5BF0DC26A2DB1E4CDB1050E`;
- `ma01-prompt.txt` SHA-256 `C8809FCEF1BBDB95F6C8952E1F14DDC54611A90AA84A656EB6F20CA35A7F4FF5`;
- `ma01-output-schema.json` SHA-256 `EFB39E17DC97B5826CA3C8017FB961C80580AB6220CD3CD56A8529567A3CBE32`.

The candidate materially addresses both first-successor falsifications. Global `plugins=false` supplements remote-plugin/apps/browser/computer/Hooks/memory/goals isolation; model-free `features`, `mcp`, and `debug prompt-input` diagnostics prove zero enabled MCP servers and zero model-visible plugin/app/MCP/Cloudflare surfaces without creating a Codex session. Root output is coordination-only; deterministic outer adjudication owns measurements and admission PASS/FAIL.

Containment is no longer sampling-based. A Windows Job Object with `KILL_ON_JOB_CLOSE` creates the root suspended, assigns it before resume, self-tests the exact kernel mechanism immediately before the live boundary, accounts job membership, kills on timeout/failure, and requires a zero-member postcondition.

Model-free preparation proof is green: parser/AST PASS; synthetic valid evidence PASS; 11 behavioral mutation cases fail closed; 8 outer containment/stability mutation cases fail closed; consumed first-successor evidence is rejected as expected; preparation interlock without opening authority exits nonzero with evidence remaining zero. No live execution has occurred and `evidence/` is empty.

## Frozen read-only task and runtime boundary

Scout A may inspect only fixture `CURRENT_STATE.md` and `docs/PROJECT_EXECUTION_QUEUE.md`. Scout B may inspect only fixture `docs/PROJECT_PARALLEL_AGENT_OPERATING_MODEL_DIRECTOR_AMENDMENT_2026_09_13.md` and `docs/blueprint/CODEX_ADMINISTRATOR_RUNTIME_SPECIFICATION.md`. Each performs exactly its sealed measurement command and exactly one sealed write probe expected to be denied by filesystem policy. Neither may inspect the fixture manifest, preparation/adjudicator machinery, another Scout's output, any repository outside the fixture, or any external surface.

The root must spawn exactly two `scout` children before its first wait. No replacement, retry, Reviewer, Worker, third child, grandchild, or autonomous next-task pickup is permitted.

For this bounded run only, CLI overrides may enable `agents.enabled=true`, `multi_agent=true`, `multi_agent_v2=false`, at most two concurrent threads, and depth 1. The exact named Scout remains GPT-5.6 Sol / medium / read-only. Approval is `never`; the execution sandbox is read-only. Plugins, remote plugins, apps, browser, computer use, Hooks, memories, goals, web search, and connector/app surfaces remain disabled. This creates no project-provider authority and no E0-D authority.

## Required deterministic PASS evidence

PASS requires every item below from the one authorized execution:

1. exactly one root session and exactly two unique depth-1 Scout sessions; no extra child or grandchild;
2. both Scouts are spawned before the root's first wait and their final-turn active intervals overlap, proving logical concurrency;
3. child rollout metadata proves the exact root parent, depth, `scout` role, fixture cwd, Codex version, model/reasoning, approval-never, read-only sandbox and restricted-network inheritance;
4. each Scout executes exactly the sealed two-command contract and no other tool/action surface;
5. all four SHA-256, byte and LF measurements exactly match the sealed fixture manifest;
6. each write probe reaches filesystem policy, is denied, and leaves no probe file;
7. rollouts/stderr contain no plugin skill/instruction surface, MCP/remote-MCP startup, URL/network, Git, provider, browser, app, Hook, authority or outside-root activity;
8. the root's schema-valid final JSON is coordination evidence only; deterministic outer adjudication returns PASS independently;
9. the supervisor completes within 180 seconds; Job Object self-test passes; root is assigned before resume; at least one job member is observed; zero job members/survivors and zero new post-run `codex.exe` remain;
10. exactly the expected three new session/rollout records are attributable and copied/sealed;
11. candidate artifacts, fixture bytes, Codex, PowerShell, Python, Scout profile, Administrator config/profile/runtime manifest, and copied opening-authority document all remain hash-stable after execution;
12. Project/Ryladmin/Website refs, commissioned runtime, E0-D state, provider state, Git state and validation state remain unchanged by the run.

Any missing/ambiguous evidence, malformed rollout line, unknown tool/event type, command drift, extra agent/session, wrong measurement, write-probe failure, plugin/MCP/network surface, inheritance mismatch, missing overlap, timeout, escaped/surviving process, artifact/runtime/fixture/opening-document drift, or deterministic adjudication failure is **MA-01 SUCCESSOR-04 FAIL**. Stop immediately without retry. No failure authorizes a corrected run.

## Promotion, local opening interlock, consumption and stop rule

This document may be promoted only through the normal Project branch/PR path. Before live execution, its exact opening head must pass hosted exact-head Validation, it must merge to Project `main`, and push-triggered exact-main Validation must pass on that exact merge.

Only after those gates may the Administrator construct local `opening-authority.json` schema `ensemble.ma01.opening.v2`, binding this candidate manifest SHA-256, the exact opening head, exact authoritative Project main, exact-head and exact-main Validation run IDs, this repository evidence path, and a byte-exact copied opening document/hash. The supervisor must independently verify that interlock and all sealed artifacts before consumption.

Immediately before launch, fresh-resolve Project/Ryladmin/Website refs; prove they match the authorized opening baseline except for the opening's own validated Project transition; rehash every sealed candidate/proof/runtime dependency and fixture; rerun the exact model-free config/prompt preflight; prove evidence/probe residue zero; prove no preexisting `codex.exe`; prove commissioned agents/multi-agent remain default-off; and pass the Job Object self-test. Any mismatch stops before consumption.

The authorization is consumed exactly when the sealed supervisor launches the sealed runner after all non-consumptive checks pass. The supervisor may be invoked at most once with valid opening authority. Afterward, seal/adjudicate the terminal evidence and reconcile Project authority before any runtime admission or later MA gate.

A PASS may support a later separately audited read-only Scout runtime-admission decision. It does not itself alter the commissioned default, authorize MA-02+, Worker children, Reviewer repair, Claude subagents, Hooks, Automations, Git/provider activity, or autonomous work pickup.
