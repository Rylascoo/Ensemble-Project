# E0-A Second Real Gemini 3.5 Flash-Lite — Director Authorization

Status: **AUTHORIZED — EXACTLY ONE CORRECTED SECOND REAL RUN — SYNTHETIC MISSING RAFT ONLY**

Date: **2026-09-08**

## Director authorization

The Director stated:

> I confirm no other material Gemini API traffic has occurred since my last confirmation, and I authorize exactly one corrected second real synthetic Missing Raft `GEMINI-3.5-FLASH-LITE-MINIMAL` run at promoted checkout `689655eed677b789ab3ee395f1c65b4f2cb72cc8`.

## Exact scope

This authorization permits one invocation of the E0-A live host using:

- executable checkout: `689655eed677b789ab3ee395f1c65b4f2cb72cc8`;
- validation tag: `validation/e0a-gemini-counttokens-correction-native-arm64`;
- fixture: canonical synthetic Missing Raft fixture only;
- arm: `CREATIVE-MINIMAL`;
- provider profile: `GEMINI-3.5-FLASH-LITE-MINIMAL`;
- run id: `E0A-REAL-G35L-20260908-02`;
- exactly one real host invocation;
- real Gemini `countTokens` preflights and generation/inference required by that run;
- existing 15 RPM / 250K input TPM / 500 RPD discipline;
- one attempt per role invocation and zero automatic retries;
- existing $5 shadow-spend ceiling and evidence-sealing laws.

Any terminal result consumes this authorization. It does **not** authorize a retry, a third real run, another fixture, another model/profile, mixed-model routing, fallback, or unrelated provider traffic.

Free-tier execution remains synthetic-fixture-only. The replacement API key must remain process-local/environment-only and must not be committed, printed, pasted into chat, or written into evidence.

## Preconditions established before authorization

- annotated validation tag object `7337ec6c6ff7c6d9d12c9a1322d0c5981e5fdb83` dereferences exactly to the authorized checkout;
- Director Windows ARM64 validation at that checkout: Core 622/622 and Harness 125/125 PASS, fresh Harness build and fixture smokes PASS;
- current credentialless live-route gates PASS and retired 2.5 Flash rejection PASS;
- first real attempt 01 was audited and its `countTokens` request-contract defect was corrected before this authorization;
- current account evidence: Gemini 3.5 Flash-Lite Free tier = 15 RPM / 250K input TPM / 500 RPD;
- Director confirmed no other material Gemini API traffic since the prior confirmation;
- official Google provider documentation was rechecked on 2026-09-08 for project-level rate-limit semantics, current Free-tier pricing/data-use state, and `minimal` thinking semantics;
- executable snapshot freshness gate remains valid through 2026-09-14 UTC.

## Evidence destination

The one-run packet must use a new non-colliding local evidence root derived from run id `E0A-REAL-G35L-20260908-02` under the established local provider-run evidence directory, preserve stdout/stderr/transcript and a sealed evidence ZIP when produced, and remove provider credentials immediately after the invocation.

## Post-run boundary

After this invocation terminates for any reason, no further real provider run is permitted until this result is audited and the Director makes a new explicit decision.
