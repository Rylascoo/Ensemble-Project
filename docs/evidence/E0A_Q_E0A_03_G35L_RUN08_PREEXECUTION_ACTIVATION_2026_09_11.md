# E0-A Q-E0A-03 Gemini 3.5 Flash-Lite Run 08 - Preexecution Activation

Date: 2026-09-11

Status: **PREEXECUTION GATES PASS - OPTION A + STANDING DIRECTOR AUTHORITY - PROVIDER TRAFFIC NOT YET SENT**

Authority: `docs/evidence/E0A_Q_E0A_03_OPTION_A_DIRECTOR_DECISION_2026_09_11.md` authorizes exactly one fresh 3.5 Standard/Free replacement. `docs/evidence/GEMINI_API_PROJECT_RELEVANCE_STANDING_DIRECTOR_AUTHORIZATION_2026_09_10.md` supplies the exact-run provider-use authorization mechanism after this activation is integrated and post-merge Validation is green. Frozen no-retry/no-fallback, evidence, contribution, synthetic-only, deterministic-authority, and hard-stop law remain binding.

## Exact replacement boundary

- RunId: `E0A-Q03-G35L-20260911-08`
- executable: `bb869fb1c505603612bc718f739b3f1b358e5539`
- validation tag: `validation/e0a-gemini-technical-failure-diagnostic-classification-native-arm64`
- tag object: `c4ff8dc4b9ebd31674208f4ba9c958d50fc975d5`
- fixture: `ensemble.e0.missing-raft@0.1.0`
- canonical fixture SHA-256: `5556a02325e6a7f774e6997942b395d670741d494ea86f1a50b83633e26b6703`
- fixture Git blob: `6c2ed0e1081ee4e1165dbfb162b3028cbd1136fd`
- profile: `GEMINI-3.5-FLASH-LITE-MINIMAL / CREATIVE-MINIMAL`
- model: `gemini-3.5-flash-lite`
- role controls: Performer minimal / Integrity high / Interpreter minimal
- accepted-turn cap: `12`
- evidence root: `C:\Users\Wiryl\Sol Dev\Ensemble-Project-Evidence\E0A-Q03-G35L-20260911-08`

The run envelope remains one attempt per probabilistic role invocation, zero automatic retries, no fallback/model/profile/fixture substitution, 300-second attempt timeout, 4096 visible-token ceiling per role, USD 5 shadow-spend ceiling, synthetic-only privacy boundary, immutable evidence, and existing rate/usage/cancellation law.

## Integrated prerequisite

Option A was integrated by PR #84 as `ceb057f9b7aa66530044b6bcc4af1385e61e6840`. Exact-head Validation #681 and E0-E preparation #38 passed; post-merge Validation #682 passed. Archive tag `archive/q-e0a-03-option-a-director-decision-2026-09-11` peels to `8aa10293daf433576a3b496541efae673686ab4c`.

Native machine-test authority remains exact checkout `bb869fb1c505603612bc718f739b3f1b358e5539`; later documentation and merge commits do not inherit native runtime authority.

## Current provider/account gate

Official Google Gemini documentation was rechecked on 2026-09-11. Exact `gemini-3.5-flash-lite` remains a stable Gemini API model with 1,048,576 input tokens, 65,536 output tokens, Structured Outputs support, and Thinking support; no shutdown date is currently announced. Standard Free Tier input/output remain free of charge, while current Standard paid shadow rates are USD 0.30 / 1M input, USD 0.03 / 1M cached input, and USD 2.50 / 1M output including thinking. Free-tier submitted content remains eligible for use to improve Google products, so the route remains synthetic-only.

Those facts match the exact validated executable profile and its pricing assumptions. `E0AGeminiPricingPolicy.SnapshotValidThrough = 2026-09-14`, so the fail-closed freshness guard remains valid on 2026-09-11. No source correction or renewed native validation is required by the current public-provider recheck.

The intended authenticated AI Studio route remains the same Kymaean project and protected testing key. Director-supplied same-day UI evidence confirms Free tier and the exact 3.5 Flash-Lite limit row at **15 RPM / 250,000 input TPM / 500 RPD**. The reusable 3.5 quota decision remains valid: Runs 06/07 produced no `429` / `RESOURCE_EXHAUSTED` or other quota contradiction, and no project/key/tier/model/routing change has been introduced.

The usage ledger records 52 known 3.5 provider operations dated 2026-09-11 when Runs 03/04/05/06 are counted. The full replacement has a conservative 72-operation maximum, so even a same-window bound is `52 + 72 = 124 <= 500 RPD`. No compatibility, availability, key-health, or quota probe is justified; Run 08's first provider operation must be its own `countTokens` preflight.

## Native/local activation

Preserved validator `C:\Users\Wiryl\Sol Dev\E0V-bb869fb` is detached and clean at the exact executable; the validation tag dereferences exactly to that checkout. Repository `global.json` selects SDK `9.0.317`. A fresh Release `win-arm64` Harness build passed with zero warnings/errors, the canonical Missing-Raft smoke exited zero with `Fixture validated: ensemble.e0.missing-raft@0.1.0`, and the checkout remained clean.

RunId `E0A-Q03-G35L-20260911-08` has no active matching process. Its evidence root does not exist. Its immutable run-claim and root-claim names under `.ensemble-e0a-claims` also do not exist. The activation package creates neither claims nor runtime evidence.

A corrected credentialless exact-profile invocation exited `1` with `GEMINI_API_KEY is required at the E0-A provider edge.` and created no evidence root or claims. Two earlier wrapper-only probes did not reach credential validation: this PowerShell host exposed a null `ProcessStartInfo.ArgumentList`, and `Start-Process -ArgumentList` split the evidence path at spaces. Both exited before provider work and created no root/claims; they consumed no credential and no provider operation.

The protected CurrentUser credential blob and ready marker are present outside the repository. A fresh SecureString conversion succeeded without printing, hashing, persisting, or exporting plaintext. Ambient `GEMINI_API_KEY` is absent. At execution, plaintext may exist only transiently in memory and only the intended Harness child environment may receive it.

## Scope relevance and consumption optimization

Option A exists specifically to test one bounded replacement after separate 3.5 and 3.1 Standard-route availability-class 503 terminals prevented either planned full candidate from contributing. Run 08 asks whether the same admitted 3.5 Standard/Free reference configuration can reach the frozen 12-turn contribution boundary without changing retry, model, service tier, fixture, or deterministic semantics.

A standalone availability, compatibility, quota, or credential probe would consume provider capacity without answering that reference question. The lowest-call useful next action is exactly one full Run 08 execution after this activation is durably integrated. Preexecution provider consumption for Run 08 is **0 calls**.

## Contribution and hard-stop law

Run 08 contributes only if all 12 Turns reach accepted Take, causal commit, and following deterministic Opportunity; provider receipts/model identity/provenance remain valid; no technical, cancellation, budget, provider-usage-policy, cache, or Integrity terminal occurs; the runtime artifact seal is complete; and independent post-run hard-gate evaluation passes.

Any actual Harness claim/root creation consumes this RunId and evidence root. Any terminal provider/runtime result remains immutable evidence and must be recorded in `docs/evidence/GEMINI_API_USAGE_LEDGER.md`.

If Run 08 terminates technically before 12 accepted turns, **no second Standard/Free replacement is earned under Option A**. Provider traffic returns to zero and a materially different Director route/redesign disposition is required before any further provider call. No retry, replay, fallback, or silent substitution is permitted.

If Run 08 reaches 12 accepted turns, contribution still requires the independent hard-gate review and valid evaluation seal; reference selection must retain and report all prior noncontributing runs. The 2.5 control remains outside Option A.

## Disposition

Run 08 is **READY FOR ONE EXECUTION** only after this activation package is integrated and post-merge hosted Validation is green. At that point Option A plus the standing project-relevance Gemini authorization satisfy the route/use authority for this exact fresh run. This record itself sends no provider traffic and authorizes no second replacement, paid/Priority route, retry, fallback, model substitution, or 2.5 advance.
