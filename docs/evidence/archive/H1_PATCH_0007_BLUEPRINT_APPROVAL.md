# H1 Patch 0007 — Blueprint Approval Record

Date: 2026-09-02
Status: APPROVED FOR IMPLEMENTATION; executable validation not yet established

Canonical blueprint:
`docs/blueprint/H1_PATCH_0007_DIRECTOR_OPPORTUNITY_CONTRACT.md`

Approved proposal:
**1.5**

Recursive adversarial audit status:
**COMPLETE — full zero-material-change pass achieved before approval.**

Exact audited/approved blueprint head:
`3562db371f67f07f5896ff2a17f067e0ef02e6e9`

Approval PR:
**#15 — H1: define E0 Director opportunity contract**

GitHub records PR #15 as merged at the exact approved head above. The persistent draft flag is a connector/UI artifact and is not approval authority.

## Approval scope

The approval freezes the Patch 0007 E0 Director contract described by Proposal 1.5, including:

- E0-only deterministic/model-free least-intervention reference strategy;
- singular selected Character for the current E0 spine without closing post-E0 subset attention;
- fixture-authored opening `VOSS` remaining outside Director selection;
- structurally validated least-privilege `DirectorOpportunityInput`;
- structural validation explicitly not authenticating causal opportunity-history provenance;
- semantic `DirectorOpportunityProposal` contract `ensemble.e0.director.opportunity.v1`;
- strategy contract `ensemble.e0.director.least-intervention.v1`;
- selection grammar: nomination, else addressed-pool recency, else complete-roster recency;
- structural attention diagnostics remaining trace-only and never becoming fairness/routing authority;
- no numeric weights, quotas, exclusion timers, probabilities, randomness, or LLM Director routing;
- precommit evaluation remaining discardable speculation;
- postcommit effective use requiring authoritative history plus re-Bind/recompute from source Context semantics matching accepted Candidate `ContextPacketId`;
- postcommit recomputation remaining only a proposal;
- later effective-opportunity establishment atomically coupling Current Opportunity with its corresponding opportunity-history event;
- E0-D round-robin remaining a separate strategy that may share the structural Input and semantic Proposal shape;
- no provider, Integrity, State, Take, commit, persistence, Scene-loop, World Resolver, UI, Windows AI/NPU, packaging, or Store implementation entering Patch 0007.

## Validation boundary

This approval is architecture/specification authority only.

It does **not** establish:

- compiler authority;
- test-execution authority;
- target-device runtime authority;
- effective opportunity application behavior;
- causal-history persistence;
- provider/model behavior;
- NPU behavior;
- WACK or Store authority.

Executable Patch 0007 work must begin from the approval checkpoint recorded in `CURRENT_STATE.md` and must receive its own native ARM64 validation evidence before Patch 0007 can be marked COMPLETE.
