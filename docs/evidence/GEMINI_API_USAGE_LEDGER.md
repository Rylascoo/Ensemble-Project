# Gemini API Usage Ledger

Status: **ACTIVE - PROJECT-RELEVANT GEMINI KEY**

Current authority: `docs/evidence/GEMINI_API_PROJECT_RELEVANCE_STANDING_DIRECTOR_AUTHORIZATION_2026_09_10.md`. Earlier diagnostic batches remain governed historically by `docs/evidence/GEMINI_API_TEST_KEY_STANDING_DIRECTOR_AUTHORIZATION_2026_09_10.md` and any exact run authorization recorded at the time.

This ledger records provider consumption without recording the credential. Every new batch must state its project relevance and how its result eliminates, defers, narrows, or justifies the next provider consumption.

| Date UTC | Batch / Run | Purpose | Model / endpoint | Calls | Result | Project relevance | Next-use optimization |
|---|---|---|---|---:|---|---|---|
| 2026-09-10 | `E0A-Q03-G35L-20260910-02` | Full-reference Run 02 under its then-exact one-run authorization; validate post-diagnostic generation path | `gemini-3.5-flash-lite`; `countTokens` + `streamGenerateContent` | 2 | `countTokens` PASS at 649 input tokens; generation HTTP 400 `INVALID_ARGUMENT`, field `generation_config.response_format.text.mime_type`; zero accepted turns; authorization consumed. | High: localized the next E0-A blocker after the corrected token-count path. | Never replay Run 02; use bounded diagnostics to isolate the rejected generation field before another full run. |
| 2026-09-10 | `GEMINI35-FORMAT-COMPAT-20260910-01` | Distinguish general 3.5 Flash-Lite generation failure from structured-output encoding incompatibility | `gemini-3.5-flash-lite`; `streamGenerateContent` | 3 | Bare generation HTTP 200; legacy `responseMimeType` + simple `responseSchema` HTTP 200; near-runtime `responseMimeType` + `responseJsonSchema` HTTP 200. Local summary serialization failed after responses; provider results preserved in diagnostic evidence. | High: proved the model/endpoint itself works and narrowed the defect to structured-output encoding. | Eliminate further general-availability probes; perform only one durable near-runtime capture of the corrected shape. |
| 2026-09-10 | `GEMINI35-FORMAT-COMPAT-20260910-02` | Durable capture of the working near-runtime legacy structured-output shape | `gemini-3.5-flash-lite`; `streamGenerateContent` | 1 | HTTP 200; candidate present; prompt 649 / candidate 114 / total 763 tokens; diagnostic 128-token cap ended `MAX_TOKENS`; request/response hashes preserved. | High: supplied the exact provider evidence needed for the narrow request-builder correction now native-validated at `cef3fc15...`. | No further compatibility-only probe is justified; next useful consumption is one fresh full-reference run through the corrected executable after non-approval activation gates pass. |
| 2026-09-11 | `E0A-Q03-G35L-20260910-03` | Fresh 3.5 Flash-Lite full-reference candidate through the corrected GenerateContent path | `gemini-3.5-flash-lite`; 21 `countTokens` + 21 generation requests | 42 | Six accepted turns; all 21 generation receipts succeeded; turn 7 terminated `InvalidOutput` after schema-valid Interpreter JSON violated deterministic `characterGoal.targetCharacterId=null` law. Generation usage: 36,222 input / 6,121 output including 4,811 reasoning / 0 cached; shadow estimate USD 0.02616910. | High: first repeated full-runtime proof of corrected structured output, provider identity/accounting, and six causal commits; localized the next blocker to Interpreter prompt/semantic-contract alignment rather than provider compatibility. | Do not replay Run 03 or start 3.1/2.5. Consume zero further provider calls until the Interpreter semantic-output instruction is aligned with existing parser law and machine-validated; then use one fresh 3.5 full-reference RunId. |
| 2026-09-11 | `E0A-Q03-G35L-20260910-04` | Fresh 3.5 Flash-Lite full-reference candidate through the native-validated Interpreter semantic alignment | `gemini-3.5-flash-lite`; 1 `countTokens` + 1 generation request | 2 | `countTokens` PASS at 649 input tokens; Performer generation succeeded with 649 input / 130 output / 0 reasoning / 0 cached, then deterministic candidate parsing rejected display-style control IDs `Marlowe`/`Wren` against exact roster IDs `MARLOWE`/`WREN`; zero accepted turns; shadow USD 0.00051970; run consumed/noncontributing. | High: confirmed the corrected transport remains healthy while localizing the next blocker to missing exact roster-ID exposure and Performer control-identity instruction. | Never replay Run 04 or probe 3.1/2.5. Use zero provider calls until the bounded control-ID interface is audited, native-validated/tagged, integrated, and a fresh 3.5 RunId is activated. |

| 2026-09-11 | `E0A-Q03-G35L-20260910-05` | Fresh 3.5 Flash-Lite full-reference candidate through the native-validated role control-identity correction | `gemini-3.5-flash-lite`; 3 `countTokens` + 3 generation operations attempted | Performer and Integrity generations succeeded; Interpreter `countTokens` succeeded, then Interpreter generation terminated `TechnicalFailure` with no response/usage receipt; zero accepted turns; conservative shadow USD `0.16775400`; final-call provider usage unknown; run consumed/noncontributing. | High: proved canonical control IDs reached Performer acceptance and Integrity, then exposed an evidence-observability gap at the Interpreter technical-failure boundary. | Never replay Run 05. Use zero provider calls until fixed exception-class diagnostics are audited, native-validated/tagged, integrated, and a fresh run is preregistered. |
| 2026-09-11 | `E0A-Q03-G35L-20260911-06` | Fresh 3.5 Flash-Lite full-reference candidate through the native-validated technical-failure diagnostic classification | `gemini-3.5-flash-lite`; 1 `countTokens` + 1 generation operation | 2 | Performer `countTokens` PASS at 724 input tokens; first Performer generation returned HTTP 503 / `UNAVAILABLE` with no usage receipt; zero accepted turns; conservative shadow USD `0.16405720`; run consumed/noncontributing. | High: resolved Run 05's catch-all ambiguity by proving the hardened HTTP diagnostic path works and localized this run to provider availability rather than local schema/parser logic. | Never replay Run 06 or treat it as automatic 3.5 replacement authority. Under the frozen comparison sequence, next useful consumption is one fresh 3.1 full candidate only after current 3.1 account/project/key/quota and volatile provider facts are reverified and its exact activation is durable. |
| 2026-09-11 | `E0A-Q03-G31L-20260911-07` | Fresh 3.1 Flash-Lite full-reference candidate after authenticated Free-tier/project/key/quota activation | `gemini-3.1-flash-lite`; 8 `countTokens` + 8 generation operations | 16 | Turns 1-2 committed; seven generations succeeded with exact returned model; Turn 3 Integrity generation returned HTTP 503 / `UNAVAILABLE` after successful `countTokens`; accepted turns 2; known successful-generation usage 10,886 input / 2,143 output incl. 1,514 reasoning / 0 cached; conservative shadow USD `0.10504725`; failed-call usage unknown; run consumed/noncontributing. | High: establishes exercised 3.1 live-route/structured-output/accounting compatibility and adds a second cross-model 503 availability-class terminal. | Never replay Run 07. Neither full candidate contributed, so stop provider consumption; do not start 2.5 or an automatic replacement. Next useful work is the separately audited correction/route decision required by the frozen closure contract. |


Run 04 activation predecessor: `docs/evidence/E0A_Q_E0A_03_G35L_RUN04_PREEXECUTION_ACTIVATION_2026_09_10.md`.

Launcher/setup failures before these batches consumed 0 provider calls.

## Consumption rule

Before provider traffic, record the exact question/run boundary and choose the lowest-call design that can materially advance the active project gate. After traffic, append actual calls/results, classify project relevance, and state the evidence-driven next-use reduction or justification. Stop when another call is unlikely to change a decision, resolve a blocker, or produce required experiment evidence.

## Retirement

Key retirement status: **ACTIVE FOR PROJECT-RELEVANT GEMINI WORK**.

When Gemini testing/experimental evidence closes, revoke or retire the key in Google AI Studio and append the retirement date and confirmation here. No key material is ever recorded in this ledger.
