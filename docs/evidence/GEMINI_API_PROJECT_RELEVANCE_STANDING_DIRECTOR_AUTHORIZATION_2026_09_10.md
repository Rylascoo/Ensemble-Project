# Gemini API Project-Relevance Standing Director Authorization

Date: 2026-09-10

Status: **ACTIVE — STANDING PROJECT-RELEVANT GEMINI API AUTHORITY**

## Director instruction

The Director authorized Engineering Sol to use the protected Google Gemini API key without returning for routine per-call or per-run permission when the use is materially relevant to already-authorized Ensemble/Kymaean project work.

Every provider-use batch must be recorded in `docs/evidence/GEMINI_API_USAGE_LEDGER.md`. Engineering must evaluate how the consumption advanced the active project scope, use the result to reduce or sharpen the next consumption, and surface a Director update when API usage produces a meaningful project-relevant development.

## Effect on Q-E0A-03 authorization mechanics

For Q-E0A-03, this decision supersedes only the separate-approval cadence in the frozen activation/closure contract. Engineering may satisfy the run-authorization condition by recording, before provider traffic, one exact fresh RunId, admitted profile/model, canonical Fixture, machine-validated executable/tag, no-retry/no-fallback boundary, and nonexistent evidence root under this standing authority.

A consumed named run remains immutable and is never replayed. One run identity never authorizes a retry, replacement attempt, fallback route, or reuse of an evidence root.

## Boundaries

- Gemini API Free tier only unless the Director later authorizes a paid route or billing change;
- synthetic/no-secret project fixtures only; no private, user-derived, credential-bearing, or production content;
- only current admitted Gemini models/endpoints and work materially connected to an active authorized project gate;
- no automatic retry loops, silent fallback, model substitution, experiment-order bypass, or provider-policy expansion;
- the key remains protected on the Director machine and must never appear in Git, chat, logs, command arguments, evidence payloads, or generated artifacts;
- provider consumption must stop when additional calls are no longer likely to change a project decision, resolve a blocker, or produce required experimental evidence;
- this authority does not itself advance phase state, select an E0 reference, approve product/provider admission, or alter scoring/contribution law.

## Consumption discipline

Before a batch, state the question the call can resolve and prefer the lowest-call design that can falsify or establish it. After a batch, record actual calls/results, classify the result's project relevance, and state what next consumption is eliminated, deferred, narrowed, or justified by the evidence.

The earlier `GEMINI_API_TEST_KEY_STANDING_DIRECTOR_AUTHORIZATION_2026_09_10.md` remains valid historical authority for the diagnostic calls it governed. This record is the current broader authorization surface for project-relevant Gemini key use.
