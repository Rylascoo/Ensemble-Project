# Q-DESIGN-24 Windows empirical package

Status: ACTIVE PACKAGE / PREDECLARED PLAN / provisional verdict **INCONCLUSIVE**.

Authority: Director implementation authorization incorporating Claude advisory disposition SAFE AFTER SPECIFIC TEST-PLAN AMENDMENTS, followed by approved Design clarification, 2026-09-23. Contract: `docs/design/app/contracts/SCENE_INITIAL_ROSTER_PRESENTATION.md`; decision: `docs/design/app/evidence/SCENE_PRESENTATION_CONTRACT_DECISION_2026_09_23.md`.

## Entry before executable work

Fresh main: `69c68001836ed984df672587cecd41d340a31337`; exact push Validation #1301 PASS. State distance 1, 2934 bytes. Fifteen live refs resolved: main, thirteen ancestral completed-work refs and preserved historical PR #226 `dc1780e46ef999eb01e214671486b83186ca04c4`. Existing historical worktrees remain preserved under Q-ADMIN-07; no cleanup/adoption. Source baseline equals .NET 10 native executable `4b2d286a4baf1bb01b222aceda26fbaae8772c15`; later evidence/docs are not new runtime validation.

One isolated primary writer: branch `codex/qdesign24-windows-2026-09-23`. Roles performed sequentially: Design records only approved clarifications; Implementation changes Windows presentation/tests; Governance maintains continuity. Application/Persistence APIs and Product semantics unchanged. No persistent names, current/active Scene, switching, lifecycle, roster mutation, endings/cardinality, Stage behavior, Character Core, provider/Performer, Opportunity/Performance/Take/Rehearsal, causal branches, ProductSpace, provider traffic, deferred-E0, final architecture, High Contrast acceptance, release/WACK/Store or Q-PROD-09. Stop before executable merge.

F11 is resolved by the Director's exact contract amendments. Uncertainty is session-only per ProductionId; B never clears/blocks A or inherits its block; navigation/theme/failed Open preserve witness/block; only successful ordinary Open of affected A removes both and announces the earlier unknown outcome transiently. Full Scene-hash collision blocks colliding actions plus New Scene in affected Production only; ordinary Open is not recovery and only collision-free authoritative replay clears that defect.

## Predeclared evidence and thresholds

- Independent golden UTF-16BE/SHA-256/code vectors; real precomputed 8-hex prefix collision identities, further extension if practical, never CI brute force. Injection only for full hash collision and not reachable in normal shipped behavior.
- Empty/multiple/identical-roster Scenes; empty Cast/roster, >5 members, Cast-order draft canonicalization, duplicate names using one full-Cast mapping across picker/review/detail/witness (including only one namesake in a roster).
- Distinct Incompatible, typed Invalid, environmental IO/access, programming non-IO and post-success presentation failure. Limit environmental catches to invocation; never rewrite known success as uncertainty.
- Bypass matrix: Back/reenter; Current Production; Home -> Productions -> same Production; keyboard shortcuts/accelerators; all shell reentry. Active block must prevent append, with authoritative journal/Scene count checks where safe. Failed Open preserves state. Prove successful ordinary Open freshly replays an externally arranged Scene addition.
- Real key input (UIA Invoke is insufficient); no selected/current row state from focus; opaque identity focus/return after code growth; no queued Enter/key-repeat resubmission after synchronous calls.
- Native SurfSeven Light/Dark, DPI 100/150/200%, Windows text 225%, 720x520 relevant combinations; 50+ Scenes/Cast, long names, conditional codes, extended Scene codes; wrapping, vertical reachability and visible focus. Preserve permissible screenshot evidence.
- Practical temporary OS lock/access/read-only/removal failure evidence without persistent security changes; injected slow call; kill-mid-call/restart diagnostic if safe/practical, no durable pending promise.
- Focused presentation tests; .NET 10 Application/Persistence/Core/Harness; ARM64 Release WinUI; repository law/census/oracle/current hosted gates. Independent immutable-ref behaviorally no-write implementation review (report effective containment), primary repairs and affected reruns. Distinct native Design acceptance.

## Director creator protocol (human results pending)

Director is independent creator evaluator. Author prepares fixtures and records observations, never scores on Director's behalf. A critical task passes only uncoached, without wrong-Scene action, position-as-identity, focus-as-currentness, invented lifecycle, or false certainty about uncertain results.

| Task | Procedure / required observation | Status |
|---|---|---|
| A Inspection / exact return | Inspect two otherwise indistinguishable Scenes including identical empty rosters; leave and return to exact prior Scene without relying on list position. | NOT RUN |
| B Establishment | Choose specified duplicate-name Characters, review initial roster, establish and locate/inspect exact returned Scene through success/focus. | NOT RUN |
| C Prefix growth | Establish preserved prefix-collision fixture; distinguish prior Scenes after codes lengthen. | NOT RUN |
| D Uncertainty | Trigger uncertainty; ask what is known and next action; reopen normally; observe matching-roster behavior. Required understanding: result unknown, reopen to inspect authoritative Scenes, matching roster cannot attribute submission. | NOT RUN |
| E Human Narrator | Hear/distinguish all-digit 8-character code, digit/E/digit code and codes whose distinction occurs late, in context. UIA names alone cannot pass; no inference to other screen readers. | NOT RUN |

Presentation failure within adopted authority -> correct and rerun affected task. Beyond-contract treatment -> Design clarification. Durable creator-authored meaning -> smallest naming Product question; durable behavior -> exact missing semantic (possibly idempotency/removal). Preference without task failure is input only.

Final verdict must be exactly one: **SUFFICIENT — SCOPE LIMITED**, **FALSIFIED**, **INCONCLUSIVE**. SUFFICIENT requires all critical creator tasks, human Narrator, native keyboard/focus/accessibility, clean Design acceptance and regressions; only bounded initial-roster inspection/establishment, never later switching/Stage/performance/lifecycle. FALSIFIED requires persistent critical failure after permissible presentation correction and demonstrated missing Product semantic. Ambiguous/missing evidence or apparatus/contract limitations -> INCONCLUSIVE. Re-arm falsifier for future consequential selection tasks.

## Results

Exact executable candidate: `bb209ac1eaf5248e5566ef240fcf5978892fe9bf`. Exact-source independent review is CLEAN after focus eligibility, native container-tab/scroll corrections, Unicode preservation and environmental failed-Open correction. Reviewer was behaviorally no-write; effective workspace-write/automatic approval review means technical write containment was not proven. No reviewer tests, mutations or escalation.

| Evidence | Result / scope |
|---|---|
| Native ARM64 environment | PASS: SurfSeven Windows build 26200, SDK 10.0.400; no runtime installation |
| Focused presentation tests | 11/11 PASS; actual Windows source linked into platform-neutral test host, not a substitute for WinUI |
| Application / Persistence / Core / Harness | 84 / 148 / 628 / 155 PASS on win-arm64; those source surfaces unchanged |
| Final WinUI Release build | PASS, zero warnings/errors |
| Code vectors | Independent Python UTF-16BE/surrogatepass + hashlib vectors; preserved real prefix collision `QDESIGN24-53141` / `QDESIGN24-81797` gives `F55024D1675E` / `F55024D1F9F5`; all-digit `QDESIGN24-4` -> `58955591`; digit/E/digit `QDESIGN24-6` -> `4E3C642D` |
| Full-hash collision | Test-only conditional injection; normal Windows project never defines SCENE_PRESENTATION_TESTS. Affected actions/New Scene blocked; unrelated Scene and Production available. No native full-hash collision or natural SHA-256 collision claim |
| Real journal / OS lock | PASS in native Windows presentation tests: external append then own append causes typed Invalid; session block prevents more append; fresh Open surfaces both. Exclusive FileShare.None journal lock prevents append, normal UI Open returns failure safely, witness/block survives; unlock + successful Open clears. No persistent security change |
| Native launch | Isolated development identity `Kymaean.QDesign24.20260923`; exact copied binary/XBF parity preserved; processMachine 0 / nativeMachine 0xAA64, responsive. No normal app/library touched; disposable registration retained for human tasks |
| Native observed flows | Dark, current 125% DPI, 1872x1178 physical window: 55 Scenes / 55 Cast reachable; exact Back return; heading visible focus; first Tab -> checkbox; Space selection; one duplicate-name member retains code in review/detail; success focuses created Scene; following Enter inspects without another append |
| Native uncertainty/fresh replay | Scene count 55 -> confirmed 56 -> external collision fixture 57 -> uncertain own append 58; disabled New Scene/Home/library attempts left 58. Distinct typed non-confirmation, focused notice, submitted empty witness, ordinary Open neutral unknown-outcome announcement; fresh replay visibly grows existing code to 12 characters |
| Native apparatus limit | Computer-use repeatedly returned null accessibility tree. Real keyboard/screenshot evidence exists; no UIA selected/current-state or screen-reader claim. Resize attempt did not establish 720x520 |
| Deferred required observations | Full 100/150/200% DPI + 225% text + 720x520 combinations; Light parity; all native bypass/shortcut/theme/other-Production combinations; queued key-repeat stress; native slow-call; kill-mid-call diagnostic; human Narrator and Director creator tasks remain NOT RUN. Unit slow-call/reentry and per-Production cases passed; no substitution for native observations |

Real further 12-hex prefix collision generation was not attempted; no injected prefix fixtures or CI brute force. The existing two real colliders distinguish late in their extended codes. Larger-code wrapping and human intelligibility remain unearned.

Measurement and retained local evidence inventory: `docs/evidence/Q_DESIGN_24_WINDOWS_EMPIRICAL_MEASUREMENTS_2026_09_23.json`. Screenshots are scratch-only native evidence, not shipping assets. Normal app data and machine display/theme settings were not changed. The isolated app is closed; registration and fixtures are retained for the Director, not silently cleaned.

## Director Task A first run and approved Design correction

The first uncoached Director Task A attempt is measured **FAIL — presentation/discoverability boundary**. The Director reported seeing Productions/Settings and the Current Production Characters/Scenes entries but, after entering them, no clear next action or enough information to know how to perform the requested Scene task. This does not falsify Scene codes because the Director never reached a meaningful code-based distinction task.

Diagnosis against the exact Windows candidate and retained native screenshots found repeated Scene-code + action rows with no visible empty/populated roster witness, **New Scene** after the long collection, and orientation that could leave view when a lower row received focus. The Director approved a presentation-only correction using existing Product truth and established APPUI composition references from Drive: stable Scenes orientation, visible top-level **New Scene**, compact per-row initial-roster preview including explicit **Empty initial roster**, and a bounded scrolling Scene collection. Drive material is visual/composition reference only; repository Design/Product authority remains controlling.

Correction implementation is bounded to Windows presentation + focused presentation tests + this Design/evidence update. Product/Persistence APIs and semantics remain unchanged. Focused presentation tests now pass **12/12** and ARM64 WinUI Release rebuild passes **0 warnings / 0 errors** on the corrected working tree. Task A must restart uncoached on a freshly isolated corrected native build before any usability verdict changes.

## Human task apparatus / exact next boundary

Retained scratch root: `C:/Users/Wiryl/Sol Dev/admin-scratch/qdesign24-native-20260923`. `activate.ps1` activates only the verified isolated identity. `fixture/fixture.csproj` references the copied exact Application/Persistence assemblies; no provider calls. `Scene Harbor` has 58 Scenes and 55 Cast, including empty/identical rosters and duplicate Marlowe names. `Other Harbor` has empty Cast/Scenes. `Collision Harbor` is untouched with `F55024D1` before growth. Facilitator runs the preserved helper with `grow-c` only during task C, then ordinary Open exposes both extended codes. Helper `external` adds one Scene to Scene Harbor behind the open UI for task D; the next explicit establishment can yield the genuine append-then-Invalid case. These are fixture operations, not product capabilities.

Director Task A was attempted once and failed at the presentation/discoverability boundary before Scene-code sufficiency could be tested; its corrected rerun is pending. Tasks B-E remain NOT RUN. Native accessibility/scaling evidence and clean Design acceptance remain required before any sufficient verdict. No executable merge is requested. Next action is a freshly isolated corrected native build followed by uncoached Task A rerun; retain INCONCLUSIVE until the required human/accessibility evidence is earned.
