# E0-A First Real Gemini 3.5 Flash-Lite — Director Authorization

Status: **AUTHORIZED — EXACTLY ONE REAL RUN — SYNTHETIC MISSING RAFT ONLY**

Date: **2026-09-08**

## Director authorization

The Director stated:

> I confirm no other material Gemini API traffic has been made against the Kymaean project since the quota evidence, and I authorize exactly one real synthetic Missing Raft `GEMINI-3.5-FLASH-LITE-MINIMAL` reference run at promoted checkout `3d6d8a7f1caf548c15f0f2393d0fc7b50ac0cd99`.

## Exact scope

This authorization permits one invocation of the E0-A live host using:

- executable checkout: `3d6d8a7f1caf548c15f0f2393d0fc7b50ac0cd99`;
- fixture: canonical synthetic Missing Raft fixture only;
- arm: `CREATIVE-MINIMAL`;
- provider profile: `GEMINI-3.5-FLASH-LITE-MINIMAL`;
- one reference run only;
- real Gemini `countTokens` preflights and inference required by that run;
- existing 15 RPM / 250K input TPM / 500 RPD discipline;
- one attempt per role invocation, zero automatic retries;
- existing $5 shadow-spend ceiling and evidence-sealing laws.

It does **not** authorize another run after any terminal result, a retry after technical failure, another fixture, another provider profile/model, mixed-model routing, expanded data use, or any unrelated provider traffic.

Free-tier execution remains synthetic-fixture-only. The API key must remain process-local/environment-only and must not be committed, printed, pasted into chat, or written into evidence.

## Preconditions established before authorization

- promoted native validation tag `validation/e0a-gemini-rpd-model-selection-native-arm64` dereferences exactly to the authorized checkout;
- Director Windows ARM64 validation: Core 622/622 and Harness 122/122 PASS, fresh Harness build and fixture smokes PASS;
- credentialless gates for all current live profiles PASS, retired 2.5 Flash rejection PASS;
- current account evidence: Gemini 3.5 Flash-Lite = 15 RPM / 250K TPM / 500 RPD, Free tier;
- Director confirmed no other material Gemini API traffic since that quota evidence;
- pricing/data-use snapshot remains freshness-gated in the executable through 2026-09-14 UTC.

## Post-run boundary

After the authorized host invocation terminates for any reason, this authorization is consumed. No second real provider run is permitted until its evidence is audited and the Director makes a new explicit decision.
