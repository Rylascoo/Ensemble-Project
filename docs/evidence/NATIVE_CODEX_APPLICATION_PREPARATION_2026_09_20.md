# Native Codex application preparation evidence

Package: Q-ADMIN-06. Director's attached **ENSEMBLE APPLICATION — NATIVE CODEX PREPARATION AND ENGINEERING RESUME**, 2026-09-20, authorizes this operating-model reconciliation, project configuration/readiness tooling, validation, commit/push and draft PR, but not Product implementation or merge. Primary writer: this isolated native Codex task. Bounded explorers returned read-only Product and environment findings; final adoption stays with the primary/Engineering role.

## Exact authority

- Live Project main/base: `daf5f541bb9caa73e249aa8a0ba219b34730a206`; initial worktree clean/detached, now branch `codex/native-application-preparation-2026-09-20` in `C:/Users/Wiryl/.codex/worktrees/5d1e/Ensemble-Project`.
- Git common directory: `C:/Users/Wiryl/Sol Dev/Ensemble-Project/.git`; origin `https://github.com/Rylascoo/Ensemble-Project`. All ten live heads resolved with `git ls-remote --heads`, not inferred from tracking labels. Exact refs, 30 registered worktrees and owner-context read-only status: `docs/evidence/NATIVE_CODEX_APPLICATION_PREPARATION_CENSUS_2026_09_20.json`.
- Authority recovery measurement PASS; initial state distance 1, 2,493 bytes. Its `UNEXPLAINED_WORKTREE` warning was reconciled by the census, not treated as disposal permission. All 29 other worktrees preserved. Sandbox ownership refusals were followed only by narrowly scoped owner-context status/git-dir/common-dir reads; no safe.directory/ACL/ownership changes.
- Eight non-main live refs are ancestral historical residue; PR #226 is the one diverged producer. No cleanup here; Q-ADMIN-07 tracks owner/evidence/archive disposition. Two old E0-C worktrees contain staged evidence (`e0c-p01-blind-score-2026-09-13`, `e0c-p01-unblind-closeout-2026-09-13`); exact paths/status are in the census. Clean other worktrees are not released by this observation.
- Project exact-main Validation run [35501978680](https://github.com/Rylascoo/Ensemble-Project/actions/runs/35501978680) succeeded with all eight jobs and executable steps. Compiler/x64 hosted results are not native-runtime authority.
- Live Ryladmin main: `b2fff3573f27aeed2c81028febe7a662aa1f43fb`. [Q-ADMIN-05 closeout](https://github.com/Rylascoo/Ryladmin/blob/b2fff3573f27aeed2c81028febe7a662aa1f43fb/docs/Q_ADMIN_05_APP_DESIGN_AUTHORITY_SEPARATION_CLOSEOUT_2026_09_20.md) explicitly says CLOSED/COMPLETE and binds Project `daf5f...`, Website `df60232878d1faa71832abb78ee21449b7cf47e0`, receipt SHA-256 `eccdea2f526c9fe6a192bcb9827e7ddf2915c37dc58385ad5eb480b007b906c6`.
- [Ryladmin PR #90](https://github.com/Rylascoo/Ryladmin/pull/90) merged producer `03e8da37193657ef8a620afc88871d499a3b3d6a` to that main. [Exact launch-gate evidence](https://github.com/Rylascoo/Ryladmin/blob/b2fff3573f27aeed2c81028febe7a662aa1f43fb/docs/ADMIN_EST_LAUNCHGATE_01_EVIDENCE.md) retains live-maintenance/installation blocks, 0/5 real launcher admission, C10 falsified, C11 unearned and recurring automation NOT READY. Its stale draft/next-audit wording is distinguished from actual merged status; merge is not capability admission. Ryladmin was not modified or executed.

## Hold reconciliation and producer disposition

Q-ADMIN-05/global transfer wait is satisfied. The new Director decision separately authorizes native supporting agents/read-only review and retires autonomous API-driven orchestration/persistent Engineering deputy mechanics. Old custom-runtime Reviewer/MA/C failures remain historical, unpromoted, and are not native Application prerequisites. Old Product mutation leases are not reopened in this setup PR.

PR #226 disposition **RESUME**: exact producer `dc1780e46ef999eb01e214671486b83186ca04c4`; base `c8da0c1f42a40d7e3f492e0e76b343d648c69bd6`; main has 19 unique commits and producer one. Main `src/` and `tests/` have zero changes from that base. Delta is seven Application/test files, 476 insertions/5 deletions. The corrected `WorldCurrentTruth`/`CreatorReplacedWorldCurrentStateEvent` contract is immutable/canonical, creator-specific, model-neutral and does not disclose truth to Characters. No source defect requiring reconstruction was identified in the bounded review.

PR body is stale: it still names `7b34b1e...` and superseded Fact/event names. The [refined authority audit](https://github.com/Rylascoo/Ensemble-Project/issues/223#issuecomment-5738162566) and [corrected final return](https://github.com/Rylascoo/Ensemble-Project/issues/223#issuecomment-5739266383), plus exact source, supersede it. Annotated `validation/q-prod-01-application-world-current-truth-creator-provenance-native-arm64` peels to `dc1780...`; its historical 51/85/628 results are not rerun/adopted by this setup. PR/Issue metadata was read only.

Current build-ahead Product amendment permits provisional construction; exact original frozen Blueprint was not located in this checkout, so only the recovered durable audit chain is claimed. Current transferred native handoff/disclosure contracts preserve situation-first presentation, one authoritative Production and separate knowledge scopes. Home A/B and FIRSTUSE remain unresolved/pending; they do not block the nonvisual producer. DESIGN_ARCHITECTURE_READY remains NOT READY. Provider/deferred-E0/final-architecture/Alpha/release/WACK/Store and future UI gates remain independent.

Next package and acceptance are in `docs/NATIVE_CODEX_APPLICATION_WORKFLOW.md`: QPROD01-NATIVE-PRODUCER-01 from fresh post-integration main, exclusive native writer, exact-source revalidation and review. After its integration, separately implement the missing Persistence consumer (corrected [Issue #225](https://github.com/Rylascoo/Ensemble-Project/issues/225#issuecomment-5739266932)). Do not merge/adopt/discard #226 or mutate its held worktree from setup.

## Configuration and environment verification

Codex CLI reports `0.155.0-alpha.9.2`; current task exposes native supporting subagents. Official [configuration](https://learn.chatgpt.com/docs/config-file/config-reference) and [subagent](https://learn.chatgpt.com/docs/agent-configuration/subagents) documentation fetched 2026-09-20 support the authored fields and built-in worker/explorer. Standalone reviewer is defined with read-only/no-escalation defaults. No claim that a running parent override cannot widen effective child permissions. Fresh-task reviewer isolation remains a required effective-policy check, not a reproduced containment experiment.

Installed strict app-server config parsing and `config/read` in a disposable credential-free CODEX_HOME accepted the authored defaults: workspace-write, on-request, network false, web disabled, agents enabled/max 2. User config was not altered. Initial `--strict-config features list` failed because that command does not support strict mode; switched to supported app-server parsing. Initial empty input caused an EOF JSON diagnostic; the valid initialize/config-read exchange succeeded, exit 0. An isolated Doctor diagnostic reported configuration loaded but expected missing-auth/connectivity failures; these are not the real Desktop account's authentication state. No model turn, API-key route or Product-provider inference was invoked. Documentation/network probes were scoped reads.

Actual host: Windows 11 build 26200, ARM64 OS/PowerShell/.NET, selected SDK 9.0.317, Windows SDK 10.0.26100.0. Existing pinned NuGet packages supply WinUI/WindowsAppSDK and MSTest. Ten solution projects. No missing machine-wide build prerequisite established; Python absent PATH, bundled CPython used for guards. No installation/upgrade. Runtime package registration/native UI launch were not established by this package.

Desktop local environment/actions are **specified, not UI-configured**. Official [local-environment documentation](https://learn.chatgpt.com/docs/environments/local-environment) assigns generation to Settings; exact setup and four action commands are ready in the workflow document. No guessed environment schema, API runtime, packaging action or Website action was added.

## Observed validation and preserved failures

- Readiness PowerShell parse: both scripts PASS; live offline readiness PASS (10 projects), including invocation from outside the repository. Injected x64 dotnet RID is refused. First implementation run exposed a multi-PropertyGroup XML selection error; selecting the exact TargetFramework node fixed it, then readiness passed. No test failure was hidden by retry.
- Test wrapper injected first-command failure exits nonzero and does not run the second project. Native full action PASS: Application 38/38, Persistence 85/85, zero skipped/failed. First sandbox run failed **before tests**: unreadable user NuGet.Config prevented MSTest.Sdk resolution. Scoped owner/cache/network execution restored dependencies and passed. No source/test changes or flaky reruns.
- Explicit WinUI ARM64 Release build PASS, 0 warnings/0 errors. No app launch, package install, hardware, WACK or Store evidence. Product source/test bytes remain baseline; these are preparation/action checks, not adoption of PR #226 or promotion of current validation tags.
- Repository law and diff checks PASS; oracle guard PASS (670 documented hashes, 17 asserted, 653 document-only). First census found two newly orphaned historical provenance documents after simplifying AGENTS/current state. Explicit historical first-slice/charter references were restored in the workflow, without promoting their old roles. Final staged census PASS: 510 inventory, 389 current, 30 historical, 91 archive, zero unexplained. Base CI normalization PASS: complete one-run input, Validation #1100 / 35501978680. Final live Project/Ryladmin/main/producer race check unchanged. Independent review follows on the committed candidate.

No `src/**`, `tests/**`, Design receipt/contracts, Website or Ryladmin files changed. Historical orchestration documents are scoped by explicit successor notices; wholesale historical-law rewrites are deferred. AGENTS retains Product, recovery, authority, hygiene, residency and validation rules while replacing obsolete permanent-agent mechanics.

## Independent review and candidate closure

Independent read-only review of `daf5f541bb9caa73e249aa8a0ba219b34730a206` -> `af0970c80aeeaa06b1c34dac48224acde788acaf` found no actionable findings across all 15 files. It independently checked state size, diff whitespace, source parity and environment/project agreement. This was instruction-bound native supporting review, not proof of enforced custom-reviewer containment; it did not rerun tests/builds or validate PR #226. Final closure adds this evidence and queue status only; executable/configuration content is unchanged from that reviewed commit.

The prior policy tag is annotated and peels to `c352d96fc3a10ea5e0a517c4057e4173000288d3`; no subsequent baseline changes exist in src/tests/fixtures/global.json/Directory.Build.props/Ensemble.sln. Receipt bytes reproduce the exact closeout SHA-256 above. Native preparation is complete as a candidate; integration remains pending Director authorization, and Desktop environment/action UI configuration remains the explicit manual setup step. No merge was performed. The resulting branch/PR head and hosted checks are reported in GitHub rather than inventing a self-referential commit hash in this evidence.
