# First Product-Native Performance — Director Disposition Input

Status: **DIRECTOR-PROVIDED CHAT DISPOSITION — PENDING INTEGRATION / NO IMPLEMENTATION AUTHORITY**

Date: 2026-09-23  
Base repository authority recovered before the decision sequence: `main@f685a21a699f3c9300a8db15358aa5d5d125115f`.

This record preserves Director dispositions supplied in the continuing Ensemble Project conversation after the first-Performance read-only architecture memo and independent Claude falsification. It is continuity evidence on this branch until integrated. It does not itself authorize executable Product work, provider traffic, deferred-E0 consumption, PR #262 merge, or final architecture.

## Milestone split

The Director agrees to distinguish two milestones.

### First Product-native Performance

A deterministic test-adapter Performer **counts** for this milestone only when the real Product/Application path:

1. binds an existing Product Character and established Product Scene;
2. creates the admitted bounded Opportunity;
3. invokes the deterministic Performer adapter;
4. produces a Product candidate Performance;
5. reaches the admitted Take/consequence acceptance authority;
6. commits accepted Performance plus authoritative consequence atomically into durable Product causal history;
7. reconstructs that authority by ordinary replay; and
8. makes the consequence observable in the acting Character's own later **bounded Product context**.

Authoritative replay changing by itself is not the complete milestone.

### First live AI Performance

This is a later milestone. It requires separate provider/runtime/admission/credential/network/spend authority as applicable. The first Product-native milestone does not authorize a provider, model, credential, development provider association, network traffic, or spend.

## Director dispositions already given

These choices narrow the first Product-native Performance only. They are not final launch architecture.

### Opportunity

Use an explicit typed Opportunity bound to one existing `SceneId + CharacterId`.

Do **not** productize automatic Director selection in the first slice.

### Scene targeting

Target one existing established `SceneId` explicitly.

Do **not** introduce durable Current/Active Scene, Scene switching, Scene endings, or Scene lifecycle merely to prove the first slice.

### Take / consequence acceptance

Use a bounded deterministic **provisional** Take/consequence policy for the first provider-free Product proof.

This does **not** close ODR-19 and does not decide final creator consequence-review UX.

### Performer binding

Use an invocation-scoped deterministic Performer adapter for the first milestone.

Do **not** introduce persistent Character→provider/model/understudy casting in this slice.

## Final unresolved analysis scope

Exactly three coupled questions remain for one final read-only Product/Architecture pass:

1. **Q1 — E0 → Product acquisition mode:** bounded reuse behind Product adapters, extraction/generalization, Product-native port, or clean re-derivation from frozen contracts/oracles.
2. **Q2 — minimum Character context/access:** because the milestone requires the consequence to affect the acting Character's later bounded context, identify the smallest Product-owned context and access rule without silently adopting the whole E0 ontology.
3. **Q5 — first consequence domain:** choose the narrowest justified durable consequence family together with Q2. Character Circumstance is a candidate, not a decision.

The final pass must compare concrete code footprint and deferred-validation exposure. It may recommend one option per question with a falsifier, but it may not implement or silently settle Product authority.

## Stopping rule

The Q1/Q2/Q5 analysis is the **last architecture-analysis round before implementation**.

After the Director disposes those three returned recommendations, the next Product artifact is a bounded implementation package. Do not create another architecture memo unless the final pass proves one of the three questions cannot be answered without new evidence and names the exact blocking evidence.

## Deferred-validation requirement

Any later Q-PROD implementation that adds or changes durable Performance, Take, consequence, persistent identity, replay-derived primitive, or journal event family must satisfy `docs/ENGINEERING_HYGIENE_CONSTITUTION.md` deferred-validation exposure law before executable closeout.

The implementation must not reinterpret already accepted Product history silently if deferred E0 evidence later requires a change.

## Explicit non-authority

This record does not:

- choose the Q1 acquisition mode;
- adopt Character Circumstance or any other Q5 consequence domain;
- define the Q2 Product access rule;
- authorize provider traffic or provider/model selection;
- resolve ODR-13, ODR-19, ODR-30 or provider-admission policy;
- consume deferred E0 work;
- activate Current Production visual refinement;
- accept or merge Q-DESIGN-24 / PR #262;
- change Q-ALPHA-01;
- freeze final Product-runtime architecture.
