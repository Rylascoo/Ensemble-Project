# Codex Administrator Establishment - Director Authorization

Status: DIRECTOR-APPROVED PROGRAM / IMPLEMENTATION PREPARATION AUTHORIZED / LIVE CAPABILITIES NOT YET ADMITTED
Date: 2026-09-19 (UTC)
Queue: Q-ADMIN-04

## Decision and provenance

After the sequential read-only Ryladmin and Ensemble-Project audits, the Director approved:

> i approve everything you need to do to establish yourself as administrator and creating your own subagents to complete tasks efficiently optimizing token usage to create this app when we eventually resume the github ryladmin repository within codex

This is standing authorization to establish the Ryladmin-backed Administrator in Codex and implement the necessary bounded runtime, coordination, test, recovery, and efficiency improvements. It is not a claim that those capabilities already work. The audit findings are inputs to verify, not a substitute for implementation evidence.

Prepare and durably record the program now. Live Codex commissioning starts when the Director resumes this establishment program in Codex. The other chats remain paused; this authorization does not silently resume them or merge their Product work.

## Authority and narrow supersession

Ensemble-Project retains policy/security/orchestration law and the single execution queue. Ryladmin owns Administrator runtime implementation and continuity. Engineering Sol remains the sole accountable Engineering manager; Design Sol retains Design/brand/UI authority. The Director retains product/ODR/provider/spend/Store decisions.

This is a narrow successor to `docs/PROJECT_PARALLEL_AGENT_OPERATING_MODEL_DIRECTOR_AMENDMENT_2026_09_13.md`, `docs/PROJECT_THREE_ENGINEER_PARALLEL_OPERATING_MODEL_DIRECTOR_AMENDMENT_2026_09_17.md`, and `docs/blueprint/CODEX_ADMINISTRATOR_RUNTIME_SPECIFICATION.md`:

- The Administrator may define task-specific agent roles, prepare exact bounded work packages, and implement/revalidate the runtime needed to execute them. Routine in-scope preparation and deterministic tests do not require repeated Director permission.
- Within a Director-started Codex establishment session, the Administrator may materialize exact successor commissioning openings under the conditions below. This replaces the need for another broad permission request for each already-scoped internal step, not the need for a sealed opening or a passed prerequisite.
- Every consumed MA-01/C9A/MWR attempt remains immutable and terminal. No old opening, namespace, prompt packet or failed realization is retried or relabeled successful. A materially changed successor receives a fresh identity and its own evidence.
- C10's upstream fail-closed eligibility condition and C11's dependency on an actual C10 pass remain binding. No scheduling, unattended loop, background polling, automatic merge, automatic publication, unlimited retry, or recursive self-dispatch is enabled by this record.
- Existing product-build-ahead law, deferred E0 holds, security restrictions, validation hierarchy, and Design authority are unchanged.

## Target operating model

The Director uses one Administrator entry point in Ryladmin. The Administrator recovers authority, prepares work, selects admitted executors, reconciles results, and explains the next action; it does not become a second Engineering or Design authority.

Engineering Sol supplies accountable architecture interpretation and exact-head integration acceptance. Application, Persistence, Windows/runtime, read-only Scout, and read-only Reviewer are functional role profiles instantiated only when useful, not six permanently running agents. A named subagent cannot acquire authority merely from its name. Final decisions identify the accountable role and exact source, rather than impersonating an absent manager.

Native child inheritance must be tested on the actual admitted Codex realization. If asymmetric read/write permissions cannot be enforced, use separately admitted isolated top-level Workers, not a more privileged parent. No worker merges or edits manager-owned authority. Only the Administrator performs serialized mechanics after the owning Sol's recorded acceptance.

## Dependency-ordered establishment

1. **Foundation:** compact current state; explicit capability status; versioned task/result schemas; fail-closed launch admission; restricted argument handling; repository CI/protection using available authorized administration access. Missing access is an explicit blocker, never a reason to extract credentials or bypass controls.
2. **Containment and recovery:** repair the suspended Reviewer outside the active installation; bound process-tree lifetime and stream collection; seal all terminal evidence; require typed results. Unify recovery with canonical source and test the restored launcher from an empty disposable profile. Preserve exact installed/runtime history and rollback.
3. **Capability admission:** a new MA-01 read-only Scout successor first; repaired Reviewer/MA-02 next; writer/child inheritance only after its prerequisites. Claude subagents remain separate MA-04 scope, not implicit in Codex admission. Each capability stays disabled until its exact implementation passes and the acceptance is durably recorded.
4. **Delivery/economics:** prove one real authorized end-to-end work package and record aggregate usage, time, rework, containment, and attributable evidence. Existing MA-05/C13 evidence standards remain; no manufactured token-burning tasks or gate inflation.

These are deliverables of Q-ADMIN-04, not separate competing backlogs. Implementation may simplify existing machinery rather than add a new framework. App delivery must not wait for optional unattended automation.

## Conditional live opening contract

Before any first live successor invocation, commit and validate an immutable opening containing: fresh run ID; owning Project decision/ref; exact Ryladmin implementation/ref and hashes; tool version/hash; permitted files/tools/network; task/input hashes; capability class; process/concurrency limits; deadline; output schema; stop/cleanup conditions; and expected evidence location.

An independent admission check must re-read the same opening immediately before consumption, reject changed/missing authority, and atomically mark that opening consumed. The tested candidate cannot change its own permission/admission record or overwrite the active installation. Pass requires explicit root exit status, bounded complete-tree termination, zero owned survivors, and evidence hashes, including partial output on failure.

First prove deterministic negative cases: wrong/stale HEAD, changed packet, unavailable capability, excess concurrency/depth, protected/cross-worktree writes, unauthorized network/plugin startup, conflicting CLI options, malformed/empty results, wrapper exit before descendants, timeout, and supervisor death. Test only disposable non-secret fixtures. Live execution is prohibited until the relevant negatives pass.

Each newly opened capability class is limited to one initial live attempt and at most one materially changed corrected successor under this delegation. Failure consumes the identity and preserves evidence; repeat failure stops that class for Director disposition. There are no blind or automatic retries. A live C10 opening additionally requires the pre-existing verified upstream fail-closed trigger. C11 remains unadmitted unless its full admission contract is subsequently met.

Successor installation is separately recorded after candidate review, PR merge, rollback preparation, and isolated installation validation. The suspended production Reviewer is never used to approve its own replacement. Independent review must use an already admitted safe surface; self-review must not be labeled independent.

## Task, ownership and pause controls

Every job carries one queue/lease reference, exact baseline, accountable owner, allowed/prohibited paths, input hash, required tests, permitted validation claim, time/usage bound, cancellation behavior, and return target. Enforce one writer per worktree and one production lease per owner. The parent performs no overlapping writes. Serialize shared Git and all authority integration.

At Codex takeover, census every paused lease/branch/worktree including uncommitted changes. Record an explicit ownership transfer or preserve it as held. Do not reset, clean, amend, force-push or reopen the paused Engineer #2 candidate implicitly. No old chat and new agent may both retain write ownership over the same work package.

Project `CURRENT_STATE.md` remains the volatile integrated checkpoint. The queue is the sole backlog. Machine-readable work records carry validated operational transitions and generate/check summaries; they do not independently promote architecture or validation. Ryladmin stores runtime capability state and locators, not another Product queue.

After app work is explicitly resumed, organize authorized work as complete creator operations spanning contracts, persistence, Windows, and Design acceptance. Reproduce the audit's cold-start-recovery and post-start environmental-error findings before patching. Preserve the existing bounded Design return rather than invent a new visual program. This document does not adopt PR #226 or settle new Product ontology.

## Token and coordination economy

Optimize accepted user-visible outcomes per aggregate resource use, not agent count, raw brevity, or keeping all roles busy. Default to one executor; admit at most two simultaneous delegated jobs in total initially, with native depth one and no child spawning. Two writers require separately proven independent isolation. These are conservative program ceilings, not claims about Codex limits.

Use deterministic scripts for refs, hashes, schemas, and logs. Recover mandatory authority once per exact baseline, then read changed/required slices; never omit load-bearing bootstrap or failure evidence for token savings. Dispatch compact sufficient context with exact artifact locators, not entire chat histories. Return concise findings, changed paths, tests, risks and next owner; keep raw logs in sealed evidence.

Choose the lowest-cost currently available model/effort that meets the task's demonstrated needs. Escalate for ambiguity or consequential falsification, not routine mechanics. Model identifiers remain runtime configuration, not Product architecture. Record actual usage when exposed, otherwise label usage unknown; never invent a saving percentage or promise that parallelism lowers total tokens. A budget unsupported by the tool must be reported as an operational limit, not a technically enforced cap.

Reconcile after each bounded work package. During an active Director-started session, already-authorized internal steps may proceed after reconciliation; unrelated backlog pickup or unattended continuation is not authorized. Preserve the existing runtime's default-off capabilities until admission; don't weaken checks for throughput.

## Quota reserve and scoped-write addendum

`docs/PROJECT_ADMINISTRATOR_QUOTA_AND_WRITE_POLICY_2026_09_19.md` is the required Director-approved follow-on: approximately 10% remaining enters LIGHT, closeout reserve is protected, NORMAL requires verified replenishment, and approved changes may be authored in scoped repository worktrees. This formalizes the requested behavior; it does not enable an installed quota supervisor, override C10/C11, or resume paused Product work.

## Completion and reserved decisions

Establishment is complete only when a fresh Codex session can recover live authority from Ryladmin, execute an admitted bounded task, reject prohibited actions, collect a valid result with no survivors, and leave durable resumable state. An approved plan, green compiler, or authentic manifest alone is not completion.

Unattended operation, product-provider traffic/spend, credential changes, deployment/publication, Store submission, changes to product/Design meaning, and any unproved permission expansion remain governed by their separate authority. The user is not asked to reconstruct recoverable repository facts or reapprove ordinary steps within this program.
